# Decisions

- instruction file 生成は `Character Update Workspace` 起動時だけでなく character 保存の write path に寄せる
- 生成対象は常に `AGENTS.md` と `copilot-instructions.md` の両方
- content は provider 共通の update workspace ルールを使う
