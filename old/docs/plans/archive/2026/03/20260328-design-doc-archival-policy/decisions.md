# Decisions

- `docs/design/` には latest 実装に一致する current docs のみ残す
- `historical note` / `review note` / `supporting draft` は archive 対象とする
- archive 後も必要な参照は archive パスへ向けるが、current docs の主導線は current 正本同士に揃える
