# Decisions

- `#3` を Memory 基盤タスクとして最上位に置く
- `#14` は Memory retrieval / ranking の follow-up として `#3` の後ろに置く
- `#1` は monologue plane の provider / auth policy として `#15` の前提に置く
- `#15` は `#3` `#1` `#5` の上に載る応用タスクとして扱う
