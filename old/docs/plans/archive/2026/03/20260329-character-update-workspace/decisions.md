# Decisions

- workspace は character storage directory をそのまま使う
- update 作業そのものは通常の Session Window で行う
- WithMate は `AGENTS.md` / `copilot-instructions.md` と Character Memory extract を用意する支援側に徹する
- Character Memory extract は model を使わず deterministic に整形する
