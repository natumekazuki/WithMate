# 決定

## 2026-03-29

- current task は `Character Editor` から character 保存ディレクトリを開く導線に限定する
- `UpdateSession` 一覧は必要性があるが、先に仕様整理が必要なため別 task とする
- `Character Memory` の Editor 表示は current task では扱わない
- `Open Folder` は `Character Editor` の既存 action row に追加し、別 modal は作らない
