# Decisions

## 2026-03-27

- `Character Memory` は main の coding session prompt には注入しない
- `Character Memory` の主用途は `Character Stream` と将来の character definition update に寄せる
- `Session Memory` と `Project Memory` を coding session prompt の設計対象として扱う
