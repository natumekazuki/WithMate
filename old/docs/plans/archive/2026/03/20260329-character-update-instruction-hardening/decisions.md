# 決定

- `character-prompt-maker` のうち、current の Character Update Workspace と整合する方針だけを取り込む
- `自動 web 調査必須` のような user-driven でない要件は instruction に入れない
- `character.md` と `character-notes.md` の役割分離、既存ドラフト尊重、自己チェックは instruction に明示する
