# 20260413 SDK version build fix

## 目的

- 最新PRで更新した `@openai/codex-sdk` / `@github/copilot-sdk` に起因するビルドエラーを特定する
- 破壊的変更へ追従する修正を行い、ビルド可能な状態へ戻す

## 進め方

1. 依存更新差分とビルド失敗箇所を確認する
2. SDK利用箇所の互換性差分を洗い出して修正する
3. ビルドまたは型検査で修正結果を確認し、docs更新要否を判断する

## メモ

- 現時点では `node_modules` が未展開のため、依存導入可否も合わせて確認する
