# decisions

## status

- 進行中

## 決定

- `Home Window` と `Session Window` の minimum は `900 x 680` を採用する
- `Diff Window` の minimum は `900 x 640` を採用する
- `Diff Window` は狭幅で 2 pane 横並びを維持せず、縦 stack に倒して読みやすさを優先する
- composer attachment list は件数省略ではなく、高さ上限つき scroll を先に採用する
