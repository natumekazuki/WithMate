# Questions

## Round 1

- status: 確認済み

### 確認事項

1. `SessionStart` 独り言は、チャットより独り言のほうが新しい場合はブロックする
2. `Session close` 時の自動 `Session Memory` 生成は切る
3. 代わりに手動 `Session Memory` 生成ボタンを追加する
4. 独り言 API 運用は pending とする
5. 4 まで終わった後に通常独り言 trigger の見直しを follow-up で検討する

### 回答

- ユーザー承認済み
