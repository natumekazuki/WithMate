# 設計メモ (i-v2-g1)

- 方針: DB 正本パス選択を 1 箇所に集約する
- 根拠:
  - テストの期待値は V2 DB が存在する場合は優先的に V2 を選ぶこと
  - V2 非存在時は V1 パスを「存在有無に関わらず」返すこと
- 実装方針:
  - `existsSync` によるファイル存在チェックのみで判定する
  - ディレクトリ/ファイルの生成、移行ロジックは呼ばない
  - 起動時 `main.ts` は解決関数を呼び出すだけにする
- 既存契約:
  - IPC、preload、renderer への API 変更なし
