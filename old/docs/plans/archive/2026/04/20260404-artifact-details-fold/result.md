# 20260404-artifact-details-fold result

## 状態

- 完了

## 要約

- assistant bubble の artifact `Details` で `Changed Files` を 1 ブロックの default closed fold に変更した
- `operationTimeline` は item ごとの default closed fold に変更し、summary 1 行を先に見せる構成へ寄せた
- `Run Checks` は常時表示のまま維持した
- `docs/design/desktop-ui.md` と `docs/manual-test-checklist.md` を同期し、`docs/task-backlog.md` は `#19` を完了へ更新した
- `message-follow-banner` は message list の scroll 領域内で `sticky bottom` 表示へ移した
- 実装コミットは `bbe651d` `fix(session): fold artifact details blocks`
