# Handoff: 2026-05-05 23:27 JST

## 1. セッション概要
- 目的: SingleMate 4.0.0 の Growth / Memory / Provider Instruction Sync 周辺を、次回も継続実装できる粒度で進める。
- 現在の到達点: Memory Candidate 生成 pipeline、run workspace / heartbeat、Growth apply trigger、MateTalk fallback、Provider Instruction Target path validation まで実装してコミット済み。作業ツリーは handoff 作成前時点で clean。
- 参照ブランチ/コミット: `feat/single-character` / `8b67ff9 fix(provider-instruction): target path validation を強化`

## 2. 実施内容
- 変更ファイル: `src-electron/mate-memory-generation-service.ts`, `src-electron/mate-memory-generation-schema.ts`, `src-electron/mate-growth-storage.ts`, `src-electron/mate-memory-storage.ts`, `src-electron/memory-runtime-workspace.ts`, `src-electron/main-bootstrap-service.ts`, `src-electron/main.ts`, `src-electron/provider-instruction-target-storage.ts`, 関連 tests。
- 重要な判断:
  - Memory Candidate は schema validation 後に DB / Growth event へ保存し、relation refs、profile item refs、tag catalog / newTags、tombstone input を扱う。
  - Memory runtime workspace は `memory-runtime/runs/{runId}` の使い捨て run workspace とし、`.lock` / `.status` / heartbeat / stale cleanup を持たせる。
  - `MateMemoryGenerationService.runOnce()` は成功時 `completed`、provider / schema / JSON エラー時 `failed` を run workspace に残し、in-flight heartbeat が terminal status を上書きしないよう停止待ちする。
  - Growth apply は timer と manual IPC の両方を `MainBootstrapService.runGrowthApplyOnce()` に集約し、同時実行を単一起動に揃える。
  - MateTalk は provider-backed structured prompt の既存経路を維持しつつ、全 provider 不適合 / 失敗時は main 側で fallback 応答に落とす。`MateTalkService` 自体は injected provider failure を呼び出し元へ返す。
  - Provider Instruction Target は保存時に enabled target の `rootDirectory` 必須 / 絶対 path、`instructionRelativePath` の絶対 path / `.` / `..` / Windows drive-relative 拒否を行う。旧 DB に invalid target が残っていても sync 時に failed として記録する。
- 実行/検証結果:
  - `npm run build:electron -- --noEmit` は各実装スコープ後に成功。
  - Memory / Growth run workspace: `npm exec -- tsx --test scripts/tests/memory-runtime-workspace.test.ts scripts/tests/mate-memory-generation-service.test.ts scripts/tests/mate-memory-generation-runner.test.ts` は 40 tests pass。
  - Phase 5 broader: `npm exec -- tsx --test scripts/tests/mate-memory-generation-service.test.ts scripts/tests/mate-growth-storage.test.ts scripts/tests/mate-memory-generation-schema.test.ts scripts/tests/mate-memory-generation-prompt.test.ts scripts/tests/mate-memory-storage.test.ts scripts/tests/mate-memory-generation-runner.test.ts scripts/tests/memory-runtime-workspace.test.ts scripts/tests/provider-runtime.test.ts` は 102 tests pass。
  - Growth apply trigger / IPC / preload: targeted 17 tests pass。
  - MateTalk / runtime workspace / IPC / preload: targeted 26 tests pass。
  - Provider Instruction Target / sync / Settings UI / IPC / preload: targeted 45 tests pass。
  - `git diff --check` は各コミット前に成功。
  - `npm exec -- tsc --noEmit --pretty false --noErrorTruncation | rg "main-bootstrap-service\.test\.ts"` は、既存の全体 typecheck エラーとは別に今回追加テスト由来の残存エラーがないことを確認済み。
- active plan tier: session plan
- active plan の questions.md status: 該当なし

## 3. 残タスク
### 3.1 最優先（次回最初に着手）
1. Provider Instruction Target の禁止 root validation を main / service 層で実装する。
   - 編集対象: `src-electron/main.ts`, `src-electron/provider-instruction-target-storage.ts`, `src-electron/mate-provider-instruction-sync.ts`, 関連 tests。
   - 具体アクション: Mate source root、`memory-runtime`, `mate-talk-runtime`, temporary run directory、repository workspace 配下の target を hard fail する境界を設計し、storage ではなく userData / workspace を知る層で検証する。
2. Provider Instruction Sync の marker mismatch / duplicate marker の UX を詰める。
   - 編集対象: `src-electron/mate-provider-instruction-sync.ts`, `scripts/tests/mate-provider-instruction-sync.test.ts`, 必要なら Settings feedback。
   - 具体アクション: malformed / duplicate / target mismatch が hard fail になり、`block_session` / `warn_continue` の記録が期待通りか targeted test を追加する。
3. Phase 5 plan のチェックリストを実装済み状態に同期する。
   - 編集対象: `docs/plans/20260503-single-mate-4-roadmap/plan.md`
   - 具体アクション: 今回済ませた Memory Candidate schema/storage/run workspace/Growth apply/Provider Instruction path validation の項目を、実装済みと未完の粒度へ整理する。

### 3.2 次点
1. Growth apply の writer lock / idempotency key を実装する。
   - 編集対象: `src-electron/mate-growth-apply-service.ts`, `src-electron/mate-growth-storage.ts`, tests。
2. Provider Instruction Sync の Settings 表示に last sync state / restart required / error preview をより明確に出す。
   - 編集対象: `src/HomeApp.tsx`, `src/home-components.tsx`, `src/home-settings-view-model.ts`, tests。
3. Project Digest prompt injection の manual / integration validation を追加する。
   - 編集対象: `src-electron/main.ts`, `src-electron/session-runtime-service.ts`, `scripts/tests/session-runtime-service.test.ts`, `scripts/tests/provider-prompt.test.ts`。

## 4. 再開チェック（別端末考慮）
- 必要ツール: Node.js、npm、Electron 開発環境、SQLite 実行環境、git。
- 必要資格情報: push / GitHub 操作には Git remote の認証。Codex / Copilot provider 実機確認には各 provider の認証状態。
- ローカル状態確認コマンド:
  - `git status --short --branch`
  - `git log -1 --oneline`
  - `npm run build:electron -- --noEmit`
  - `npm exec -- tsx --test scripts/tests/provider-instruction-target-storage.test.ts scripts/tests/mate-provider-instruction-sync.test.ts`
  - `npm exec -- tsx --test scripts/tests/memory-runtime-workspace.test.ts scripts/tests/mate-memory-generation-service.test.ts scripts/tests/mate-memory-generation-runner.test.ts`

## 5. 再開手順
1. `git status --short --branch` で branch が `feat/single-character`、作業ツリーが clean、remote との差分が想定通りであることを確認する。
2. `npm run build:electron -- --noEmit` を実行して、electron 側型チェックが通ることを確認する。
3. 最優先 1 の Provider Instruction Target 禁止 root validation から着手する。まず `src-electron/main.ts` の `upsertProviderInstructionTarget` delegate と userData / workspace root 取得点を読む。
4. 実装後は Provider Instruction storage / sync / Settings / IPC targeted tests を回す。

## 6. 次回呼び出し
- `Use $session-resume to 最新handoffを読み込み、再開計画を作成して続行して。`
