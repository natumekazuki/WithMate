# Handoff: 2026-03-23 00:41 JST

## 1. セッション概要

- 目的: `GitHub Copilot CLI` の最小統合を `WithMate` の既存 Session UI に載せ、Home/Settings から provider を選べる状態まで進める。
- 現在の到達点:
  - `Codex` / `GitHub Copilot` を切り替える provider runtime を追加した。
  - `New Session` で `Coding Provider` を選択でき、`Settings` でも provider enable/disable を管理できるようにした。
  - Copilot child CLI warning 抑止と stale connection recovery を追加した。
  - plan 記録を更新し、完了済み follow-up plan を archive した。
  - ただし Electron 実機では Copilot provider がまだ `CLI server exited unexpectedly with code 0` / `Connection is closed.` で失敗するケースが残っている。
- 参照ブランチ/コミット:
  - branch: `master`
  - `5ab4ae9` `docs(plan): record copilot rollout checkpoints`
  - `f6850da` `feat(copilot): add minimal provider integration`

## 2. 実施内容

- 変更ファイル:
  - `src-electron/copilot-adapter.ts`
  - `src-electron/provider-runtime.ts`
  - `src-electron/provider-prompt.ts`
  - `src-electron/main.ts`
  - `src-electron/model-catalog-storage.ts`
  - `src/HomeApp.tsx`
  - `src/styles.css`
  - `public/model-catalog.json`
  - `scripts/tests/copilot-adapter.test.ts`
  - `scripts/tests/model-catalog-storage.test.ts`
  - `docs/design/provider-adapter.md`
  - `docs/design/coding-agent-capability-matrix.md`
  - `docs/plans/20260322-copilot-capability-rollout/`
- 重要な判断:
  - Copilot 対応は capability ごとに slice を切って進める。親 plan は `docs/plans/20260322-copilot-capability-rollout/` を維持する。
  - `New Session` では enabled provider だけを候補に出し、session 作成時に provider を確定する。
  - `Connection is closed.` と `CLI server exited ... code 0` は stale connection 系として扱い、partial result が空なら 1 回だけ retry する。
- 実行/検証結果:
  - `npm run typecheck` 成功
  - `npm run build` 成功
  - `node --import tsx --test scripts/tests/copilot-adapter.test.ts` 成功
  - `CopilotAdapter` 単体 smoke では `2+2 を数字だけで答えて -> 4` を確認
  - Electron 実機では Copilot session 実行がまだ不安定

## 3. 残タスク

### 3.1 最優先（次回最初に着手）

1. Electron 実機で Copilot provider の turn 実行を再現し、app-level でどこで connection が閉じるかを確認する。
   - 次のコマンド: `npm run electron:start`
   - 主に見るファイル: `src-electron/copilot-adapter.ts`, `src-electron/main.ts`
2. `CLI server exited unexpectedly with code 0` / `Connection is closed.` の再現条件を切り分ける。
   - session 再利用時か、新規 session でも起きるかを分ける
   - retry 前後で cached client / cached session がどうなっているかをログで確認する
3. 再現条件が取れたら follow-up plan を切り、`docs/design/provider-adapter.md` と `docs/design/coding-agent-capability-matrix.md` に反映する。

### 3.2 次点

1. rollout plan の次 slice である `session 再開 / cancel / audit parity` を切る。
   - 参照: `docs/plans/20260322-copilot-capability-rollout/plan.md`
2. Copilot provider の実機安定化後に、`Latest Command` や添付 parity の capability を順に潰す。

## 4. 再開チェック（別端末考慮）

- 必要ツール:
  - Node.js / npm
  - Electron 実行環境
  - `GitHub Copilot CLI` を使えるローカル環境
- 必要資格情報:
  - `GitHub Copilot` のログイン状態
  - 現状は provider API key を使わず、ローカルのログイン済みユーザー経由での実行を前提にしている
- ローカル状態確認コマンド:
  - `git status --short`
  - `npm run typecheck`
  - `npm run build`

## 5. 再開手順

1. `git status --short` で差分を確認する。
   - handoff 作成直後なので、次回開始時はこのファイルだけ未コミットで残っている想定。
2. `docs/plans/20260322-copilot-capability-rollout/` を読んで、親 plan の現在位置を確認する。
3. `npm run electron:start` でアプリを起動し、Home から `GitHub Copilot` を選んだ新規 session で短い text-only prompt を投げる。
4. 再現したら `src-electron/copilot-adapter.ts` と `src-electron/main.ts` の runtime を見ながら切り分ける。

## 6. 次回呼び出し

- `Use $session-resume to 最新handoffを読み込み、再開計画を作成して続行して。`
