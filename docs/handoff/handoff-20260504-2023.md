# Handoff: 2026-05-04 20:23 JST

## 1. セッション概要
- 目的: Mate 実装を再開し、Provider Instruction Sync の Settings API/UI 境界を実装して、次回作業へクリーンに引き継ぐ。
- 現在の到達点: Provider Instruction Sync の target 設定を Electron IPC、preload API、shared UI state、Settings UI、関連テストまで接続し、`80e3d14` にコミット済み。
- 参照ブランチ/コミット: `feat/single-character` / `80e3d14 feat(mate): Provider Instruction Sync 設定を追加`

## 2. 実施内容
- 変更ファイル:
  - `src/provider-instruction-target-state.ts`
  - `src/provider-settings-state.ts`
  - `src/home-settings-view-model.ts`
  - `src/HomeApp.tsx`
  - `src/home-components.tsx`
  - `src/settings-ui.ts`
  - `src/withmate-ipc-channels.ts`
  - `src/withmate-window-api.ts`
  - `src-electron/main.ts`
  - `src-electron/main-ipc-deps.ts`
  - `src-electron/main-ipc-registration.ts`
  - `src-electron/preload-api.ts`
  - `src-electron/provider-instruction-target-storage.ts`
  - `scripts/tests/main-bootstrap-deps.test.ts`
  - `scripts/tests/main-ipc-deps.test.ts`
  - `scripts/tests/main-ipc-registration.test.ts`
  - `scripts/tests/preload-api.test.ts`
- 重要な判断:
  - Provider Instruction Sync target の型を renderer/electron 共通の `provider-instruction-target-state.ts` に寄せた。
  - Settings API は `listProviderInstructionTargets` / `upsertProviderInstructionTarget` を公開する。
  - Mate 未作成時でも Settings 系 API として Provider Instruction Sync target の読み書きは許可する。
  - Settings UI では provider ごとに有効化、write mode、fail policy を操作する。write mode / fail policy は自由入力ではなく選択式にした。
  - UI の初期 target は provider ごとの既定 instruction path を使い、write mode は `managed_block`、fail policy は `warn_continue` とする。
- 実行/検証結果:
  - `npm exec tsc -- -p tsconfig.electron.json --noEmit` は成功。
  - `npm exec tsx -- --test scripts/tests/main-ipc-registration.test.ts scripts/tests/preload-api.test.ts scripts/tests/main-bootstrap-deps.test.ts scripts/tests/main-ipc-deps.test.ts scripts/tests/provider-instruction-target-storage.test.ts scripts/tests/home-settings-view-model.test.ts` は成功。
  - `npm run build:renderer` は成功。
  - `git diff --check` は成功。CRLF 変換警告のみ。
  - full `npm exec tsc -- --noEmit --pretty false` は今回未実行。前回時点では既存の広範囲型エラーがあるため、対象確認は targeted validation を使う。
- active plan tier: session plan
- active plan の questions.md status: 該当なし

## 3. 残タスク
### 3.1 最優先（次回最初に着手）
1. Provider Instruction Sync の実行処理を Settings target と接続する。
   - 編集対象: `src-electron/mate-provider-instruction-sync.ts`, `src-electron/provider-instruction-target-storage.ts`, `src-electron/main.ts`
   - 確認: enabled target だけを対象に managed-block を書き換え、sync run と target の last sync state が更新されること。
2. managed-block の実ファイル更新仕様を実装する。
   - 編集対象: `src-electron/mate-provider-instruction-sync.ts`
   - 確認: アプリ管理領域だけを差し替え、ユーザー領域を保持すること。対象ファイルがない場合は作成すること。
3. Project Digest の prompt-time injection を Provider Instruction Sync と混ぜない形で維持する。
   - 編集対象: `src-electron/session-runtime-service.ts`, `src-electron/project-digest-context-service.ts`
   - 確認: provider instruction には Project 情報を固定反映せず、送信時のみ関連 context を差し込むこと。

### 3.2 次点
1. Growth apply trigger を 1 時間程度の cadence と手動実行で接続する。
   - 編集対象: `src-electron/mate-growth-apply-service.ts`, `src-electron/main.ts`, `src-electron/main-ipc-registration.ts`
2. メイトークの provider-backed 応答と Memory 生成起動を仕上げる。
   - 編集対象: `src-electron/mate-talk-service.ts`, `src-electron/provider-runtime.ts`, `src-electron/mate-memory-generation-service.ts`
3. リセット UI/API を仕上げる。export/import は後続設計で扱う。
   - 編集対象: `src-electron/mate-storage.ts`, `src/HomeApp.tsx`, `src/settings-ui.ts`

## 4. 再開チェック（別端末考慮）
- 必要ツール: Node.js、npm、Electron 開発環境、SQLite 実行環境。
- 必要資格情報: Codex/Copilot を background provider として実行する場合は各 provider の認証状態。embedding 初回 DL にはネットワーク接続。
- ローカル状態確認コマンド:
  - `git status --short --branch`
  - `git log -1 --oneline`
  - `npm exec tsc -- -p tsconfig.electron.json --noEmit`
  - `npm exec tsx -- --test scripts/tests/main-ipc-registration.test.ts scripts/tests/preload-api.test.ts scripts/tests/main-bootstrap-deps.test.ts scripts/tests/main-ipc-deps.test.ts scripts/tests/provider-instruction-target-storage.test.ts scripts/tests/home-settings-view-model.test.ts`

## 5. 再開手順
1. `git status --short --branch` がクリーンであることを確認する。
2. targeted validation を再実行して、環境差分がないことを確認する。
3. `src-electron/mate-provider-instruction-sync.ts` から Provider Instruction Sync の enabled target 実行を接続する。
4. managed-block 書き換え、sync run 記録、Settings UI 表示の state 更新を順に確認する。

## 6. 次回呼び出し
- `Use $session-resume to 最新handoffを読み込み、再開計画を作成して続行して。`
