# Handoff: 2026-07-11 00:59 JST

## 1. セッション概要

- 目的: WithMate のゼロベース再構築に向け、Provider、Session / Run / Message、Multi-Agent の設計基準と次回の設計入口を整備する。
- 現在の到達点: Provider 接続方針、Session contract、Multi-Agent orchestration を canonical design として作成し、次回は Multi-Agent persistence 詳細設計から開始できる。
- 参照ブランチ/コミット: `feat/re-design-01`。作業開始時 HEAD は `2e3edd2`。本 handoff を含む設計変更は branch の最新 commit を参照する。

## 2. 実施内容

- 変更ファイル: `docs/design/provider-integration.md`、`docs/design/session-run-message-contract.md`、`docs/design/multi-agent-orchestration.md`、`docs/investigations/` 配下、`docs/plans/20260711-multi-agent-detailed-design-entry.md`、`docs/feature-inventory.md`、`docs/index.md`。
- 重要な判断:
  - Codex は Codex App Server、GitHub Copilot は Copilot CLI ACP server を初期接続先とし、Provider protocol を共通 contract へ変換する。
  - Session は lifecycle と粗い execution projection を永続化し、Run を詳細実行状態の正本とする。通常 Run も Provider dispatch 前に initiating Message、queued Run、projection、idempotency / dispatch record を durable admission する。
  - 通常 Session と Multi-Agent child Session を型で分けず、Hook prompt と relation で制御する。
  - Multi-Agent は複数・入れ子 child を許可し、同時実行上限を `orchestrationRootSessionId` 配下の child Run 単位 reservation として atomic に管理する。初回、差し戻し後の再開、retry のすべてが同じ admission を通る。
  - child instruction 本文の正本は child Session の initiating user Message とし、Delegation は Message ID を参照する。`mentionText` は UI 表示専用とする。
  - 子結果は child Session を正本とし、同期は明示 wait、非同期結果は ID / 状態だけを後続親 Run の Hook metadata へ通知する。
  - 指示差し戻しと評価登録を分離し、登録済み評価 record だけを集計する。
  - WithMate は Agent の file diff、merge、commit ownership を管理しない。
- 実行/検証結果:
  - Codex App Server は schema 調査と基本通信を実施済み。lifecycle、persistent Thread、resume 詳細は未検証。
  - GitHub Copilot ACP は現在の環境に契約がないため未実施。別環境向け validation plan と results template を作成済み。
  - docs の Markdown fence、末尾空白、内部リンク、古い設計判断の残存検索を確認済み。
  - build / application test は docs-only 変更のため未実行。
- active plan tier: repo plan
- active plan の questions.md status: 質問なし

## 3. 残タスク

### 3.1 最優先（次回最初に着手）

1. `docs/plans/20260711-multi-agent-detailed-design-entry.md` を読み、確定事項と完了条件を確認する。
2. `docs/design/multi-agent-persistence.md` を作成し、entity / table、constraint、index、transaction sequence を定義する。
3. root 単位の child Run capacity reservation、通常 / child Run の durable admission、Persistence Worker / actor、crash repair を具体化する。

### 3.2 次点

1. Persistence 設計を入力に Application Service / CLI operation と error contract を設計する。
2. Hook / Character payload、recovery / retention、contract test matrix を順番に具体化する。
3. Copilot 契約済み環境で `docs/investigations/github-copilot-acp/validation-plan.md` を実行する。
4. Codex App Server の persistent Thread、resume、cancel、steer、approval / elicitation を追加検証する。

## 4. 再開チェック（別端末考慮）

- 必要ツール: Git、PowerShell、`rg`、利用する Provider CLI。Memory を参照する場合は WithMate Memory CLI。
- 必要資格情報: GitHub への push 権限。Copilot ACP runtime 検証には GitHub Copilot 契約済み環境が必要。
- ローカル状態確認コマンド: `git status --short`、`git branch --show-current`、`git log -1 --oneline`。
- 事前確認チェック:
  - branch が `feat/re-design-01` であること。
  - `git status --short` で前回からの未コミット変更がないか確認すること。
  - `docs/design/` を canonical、`docs/plans/` を作業入口として扱うこと。
  - Provider runtime 未検証事項を確定済み capability として扱わないこと。

## 5. 再開手順

1. `git status --short` と `git log -1 --oneline` を確認する。
2. `docs/plans/20260711-multi-agent-detailed-design-entry.md` と参照先の canonical design を読む。
3. `docs/design/multi-agent-persistence.md` の作成から再開する。

## 6. 次回呼び出し

- `Use $session-resume to 最新handoffを読み込み、再開計画を作成して続行して。`
