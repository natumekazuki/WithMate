# Handoff: 2026-05-05 01:12 JST

## 1. セッション概要
- 目的: WithMate の単一 Mate 化を進め、Mate / Memory / Growth / Provider Instruction 周辺の設計確定内容を実装へ落とす。
- 現在の到達点: Mate 作成前ガード、Memory生成設定、Growth間隔同期、Profile Item 基盤、Profile Item の Memory 管理 UI/API 接続までコミット済み。
- 参照ブランチ/コミット: `feat/single-character` / `8ab3aca feat(mate): Profile ItemをMemory管理UIに接続`

## 2. 実施内容
- 変更ファイル:
  - `src/memory-management-state.ts`, `src-electron/memory-management-service.ts`: Profile Item を Memory 管理のスナップショット/ページング対象へ追加済み。
  - `src/memory-management-view.ts`: `mate_profile` domain の検索、ソート、フィルタ対応を追加済み。
  - `src/home-components.tsx`, `src/HomeApp.tsx`: Memory 管理 UI に Mate Profile 表示、Load More、Forget 操作を接続済み。
  - `src/withmate-ipc-channels.ts`, `src/withmate-window-api.ts`, `src-electron/preload-api.ts`, `src-electron/main-ipc-deps.ts`, `src-electron/main-ipc-registration.ts`, `src-electron/main.ts`: `forgetMateProfileItem` の IPC/API 配線を追加済み。
  - `scripts/tests/*`: Profile Item / `mate_profile` domain / IPC registration / preload API のテストを更新済み。
- 重要な判断:
  - Profile Item は UI 主操作単位とし、Forget も Profile Item 単位で扱う。
  - Memory 保存前のアプリ側 gate はスキーマ整合を主に見る。保存可否の判断は Memory 生成 LLM のプロンプト/スキーマに寄せ、返却された Memory は原則保存する。
  - Project Digest は provider instruction に直接入れず、毎回のプロンプト送信時に関連情報を検索して注入する方針。
  - Provider Instruction Sync は managed-block 前提で、アプリ管理領域をユーザー編集領域と分離する。
  - Memory生成専用 workspace は固定ディレクトリを使い、実行ごとにファイルを作り直す。並行実行は `.lock` で制御する。
  - メイトークは Mate を育てる対話画面として扱い、通常チャットと同様に Memory 生成対象にする。Mate ファイルの直接更新は行わせず、Growth/DB/同期フローに寄せる。
  - embedding は初回にローカルモデルをDLし、以後ローカルキャッシュを使う。Setting 内にDLボタンを出し、準備完了まで関連機能は実行しない。
- 実行/検証結果:
  - `npm exec -- tsx --test scripts/tests/memory-management-view.test.ts scripts/tests/home-components.test.tsx scripts/tests/preload-api.test.ts scripts/tests/main-ipc-deps.test.ts scripts/tests/main-bootstrap-deps.test.ts scripts/tests/main-ipc-registration.test.ts`
  - `npm exec -- tsc -p tsconfig.electron.json --noEmit`
  - `git diff --check`
  - すべて通過。
- active plan tier: session plan
- active plan の questions.md status: 該当なし

## 3. 残タスク
### 3.1 最優先（次回最初に着手）
1. Memory生成パイプラインを Profile Item / tag / embedding 前提へ接続する。
   - まず見る: `src-electron/memory-generation-*`, `src-electron/mate-profile-item-*`, `src/memory-management-state.ts`
   - 次の作業: LLM JSON schema、タグ一覧入力、生成結果保存、Profile Item の `remember` 相当フラグ要否を実装単位へ分解する。
2. ローカル embedding runtime / cache を実装する。
   - まず見る: `src-electron/mate-embedding-*`, `src/provider-settings-state.ts`, `src/home-components.tsx`
   - 次の作業: 初回DL、cache state、Setting DLボタン、未DL時の機能ブロック、Memory retrieval の hybrid 検索を結ぶ。
3. Provider Instruction Sync の managed-block MVP を固める。
   - まず見る: `src-electron/provider-instruction-*`, `src/home-settings-view-model.ts`, `src/provider-settings-state.ts`
   - 次の作業: DB から生成した Mate instruction projection を managed block として対象ファイルへ同期する。Project Digest は含めない。

### 3.2 次点
1. メイトーク画面を実装する。
   - まず見る: `src/HomeApp.tsx`, `src/home-components.tsx`, `src-electron/mate-talk-*`
   - 次の作業: 会話送信、Mate応答、Memory生成起動、Mate未作成時のガード、表示名/状態表示を確認する。
2. Growth apply の更新フローを実装する。
   - まず見る: `src-electron/mate-growth-*`, `src-electron/mate-profile-*`, `src-electron/provider-instruction-*`
   - 次の作業: 1時間間隔のtrigger、Profile/Bond/Work-style相当のDB更新、provider instruction sync 起動までの流れを作る。
3. Project Digest の prompt-time retrieval を設計/実装する。
   - まず見る: Project memory retrieval 系、session/companion prompt assembly 系
   - 次の作業: 現在の workspace/git 情報から関連Project情報を検索し、通常プロンプトへ注入する位置を決める。
4. reset / export / import は当面 reset のみ。
   - まず見る: `src-electron/settings-catalog-service.ts`, `src/HomeApp.tsx`
   - 次の作業: 複数端末同期前提のexport/importは後回し。reset対象にMate関連DBを漏れなく含める。

## 4. 再開チェック（別端末考慮）
- 必要ツール: Node/npm、Electron開発環境、Git、Codex/Copilot 実行環境。
- 必要資格情報: Codex/Copilot を実行するための認証。embeddingローカルDLは初回ネットワークが必要。
- ローカル状態確認コマンド:
  - `git status --short`
  - `git log --oneline -8`
  - `npm exec -- tsc -p tsconfig.electron.json --noEmit`
- 事前確認:
  - `package-lock.json` などユーザー由来の未コミット変更がないか確認する。
  - subagent を使う場合、担当ファイルを分けて root session が最終commitを所有する。
  - Provider Instruction 対象ファイルは managed-block だけをアプリが更新する。

## 5. 再開手順
1. `git status --short` で作業ツリーを確認する。
2. 最新コミット `8ab3aca` 以降の差分がないことを確認する。
3. Memory生成パイプラインか embedding runtime のどちらかを次の実装スライスとして選ぶ。
4. 実装は可能なら implementer に分割し、root session はテスト更新、統合、コミットを担当する。
5. 変更後は targeted test、`tsconfig.electron`、`git diff --check` を実行する。

## 6. 次回呼び出し
- `Use $session-resume to 最新handoffを読み込み、再開計画を作成して続行して。`
