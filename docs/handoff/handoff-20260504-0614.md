# Handoff: 2026-05-04 06:14 JST

## 1. セッション概要
- 目的: Mate の初回作成、記憶生成、Growth、メイトーク、Provider Instruction Sync、ローカル embedding を実装可能な基盤として固める。
- 現在の到達点: Mate 基盤の実装本体を `8b466bd` にコミット済み。作業ツリーはハンドオフ作成前の時点でクリーン。
- 参照ブランチ/コミット: `feat/single-character` / `8b466bd feat(mate): Mate の記憶と成長基盤を追加`

## 2. 実施内容
- 変更ファイル: `src-electron/` の Mate storage、memory、growth、provider instruction、embedding、IPC 周辺と、`src/` の Home/Settings UI、関連テストを追加・更新。
- 重要な判断:
  - Mate 未作成時は Mate 作成と Settings 以外をブロックする。
  - Memory は LLM が返したスキーマ準拠データを保存し、保存前 gate はアプリ側ではスキーマ検証中心にする。
  - Memory 生成は固定 runtime workspace に instruction files を毎回再生成し、`.lock` で同時実行を制御する。
  - Profile Item を canonical data として扱い、Mate 用 `.md` は SQL から完全再生成する。
  - Provider instruction は managed-block でアプリ管理領域を分離する。
  - Project Digest は provider instruction へ固定反映せず、プロンプト送信時に関連情報を検索して差し込む方針。
  - embedding は初回 DL 後にローカルキャッシュし、初期から hybrid retrieval を使う方針。
  - メイトークは Mate を育てる対話画面として扱う。Mate ファイルの直接更新権限は持たせず、Memory/Growth 経由で反映する。
- 実行/検証結果:
  - `npm exec tsc -- -p tsconfig.electron.json --noEmit` は成功。
  - `npm exec tsx -- --test scripts/tests/mate-growth-apply-service.test.ts scripts/tests/mate-storage.test.ts scripts/tests/mate-growth-storage.test.ts scripts/tests/mate-profile-file-renderer.test.ts` は成功。
  - `npm exec tsx -- --test scripts/tests/mate-talk-service.test.ts scripts/tests/main-ipc-registration.test.ts scripts/tests/main-ipc-deps.test.ts scripts/tests/main-bootstrap-deps.test.ts scripts/tests/preload-api.test.ts` は成功。
  - `npm exec tsx -- --test scripts/tests/mate-embedding-cache.test.ts scripts/tests/mate-embedding-download-service.test.ts scripts/tests/main-ipc-registration.test.ts scripts/tests/main-ipc-deps.test.ts scripts/tests/main-bootstrap-deps.test.ts scripts/tests/preload-api.test.ts` は成功。
  - `npm exec tsc -- --noEmit --pretty false` は既存の広範囲型エラーを含んで失敗。今回の対象確認には `tsconfig.electron.json` の targeted validation を使った。
  - `npm install @xenova/transformers --save` 後、npm audit は critical vulnerabilities を報告。未対応。
- active plan tier: session plan
- active plan の questions.md status: 該当なし

## 3. 残タスク
### 3.1 最優先（次回最初に着手）
1. Memory 生成結果を Growth event/Profile Item に接続する。
   - 編集対象: `src-electron/mate-memory-generation-service.ts`, `src-electron/mate-growth-storage.ts`, `src-electron/main.ts`
   - 確認: 生成された Memory が DB 保存後に Growth 候補として扱われること。
2. Growth apply の実行トリガーを実装する。
   - 編集対象: `src-electron/mate-growth-apply-service.ts`, `src-electron/main.ts`, `src-electron/main-ipc-registration.ts`
   - 確認: 初期値はおおむね 1 時間に 1 回。手動実行も可能にする。
3. メイトークの応答を provider-backed にする。
   - 編集対象: `src-electron/mate-talk-service.ts`, `src-electron/provider-runtime.ts`
   - 確認: MateTalk ではファイル更新権限を持たせず、Memory 生成だけを起動する。

### 3.2 次点
1. Project Digest の関連情報検索とプロンプト差し込みを実装する。
   - 編集対象: `src-electron/session-runtime-service.ts`, `src-electron/mate-memory-storage.ts`
2. Provider Instruction Sync の UI を Settings に追加し、managed-block の対象ファイルを操作できるようにする。
   - 編集対象: `src/settings-ui.ts`, `src/provider-settings-state.ts`, `src-electron/mate-provider-instruction-sync.ts`
3. リセット UI/API を仕上げる。export/import は後続設計で扱う。
   - 編集対象: `src-electron/mate-storage.ts`, `src/HomeApp.tsx`, `src/settings-ui.ts`

## 4. 再開チェック（別端末考慮）
- 必要ツール: Node.js、npm、Electron 開発環境、SQLite 実行環境。
- 必要資格情報: Codex/Copilot を background provider として実行する場合は各 provider の認証状態。embedding 初回 DL にはネットワーク接続。
- ローカル状態確認コマンド:
  - `git status --short`
  - `git log -1 --oneline`
  - `npm exec tsc -- -p tsconfig.electron.json --noEmit`
  - `npm exec tsx -- --test scripts/tests/mate-growth-apply-service.test.ts scripts/tests/mate-storage.test.ts scripts/tests/mate-growth-storage.test.ts scripts/tests/mate-profile-file-renderer.test.ts`

## 5. 再開手順
1. `git status --short` がクリーンであることを確認する。
2. targeted validation を再実行して、環境差分がないことを確認する。
3. `src-electron/mate-memory-generation-service.ts` から Memory 生成後の Growth event 接続を実装する。
4. Growth apply trigger と MateTalk provider-backed 応答を順に実装する。

## 6. 次回呼び出し
- `Use $session-resume to 最新handoffを読み込み、再開計画を作成して続行して。`
