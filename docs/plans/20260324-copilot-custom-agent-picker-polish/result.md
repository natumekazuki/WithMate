# Result

## 状態

- 完了

## サマリ

- picker に表示する custom agent を `user-invocable: true` の定義だけへ絞った
- Session UI の `Agent` ボタン自体に現在選択中の agent 名を出すようにした
- `custom-agent-discovery` テストと `build` を通した

## 次アクション

- 必要ならコミットを作成し、親 rollout plan との関係を記録する
