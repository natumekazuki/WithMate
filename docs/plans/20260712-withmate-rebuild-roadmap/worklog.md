# Worklog

## 2026-07-12: Roadmap作成

- repo planを作成した。
- `docs/index.md`、`docs/feature-inventory.md`、`docs/issue-triage.md`、最新handoff、現行design docsの後続scopeを参照した。
- checkpointをCP0からCP8の9段階に分割した。
- CP0を完了、CP1を次に着手とした。
- CP1を止める質問はない。stack / SQLite driver / Worker transportはCP1開始時に調査して確定する。
- 旧roadmapは`old/`の参考資料に限定した。

## 2026-07-12: CP1開始

- `docs/plans/20260712-cp1-runtime-persistence/plan.md`を作成した。
- CP1を`進行中`へ更新した。
- 現在地はS1 Stack / Driver / Transport Decision着手前。
- S1開始を止める回答待ちはない。

## 2026-07-12: CP1 S1完了

- runtime、package manager、SQLite driver、Worker transport、test runnerを確定した。
- CP1の現在地をS2 Project Scaffold着手前へ進めた。

## 2026-07-12: CP1 S2完了

- rootに新実装のpackage、TypeScript build、test、format、module boundary scaffoldを追加した。
- Node.js 24とElectron 42同梱Node.jsでruntime persistence probeを確認した。
- CP1の現在地をS3 SQLite Bootstrap / Schema Verification着手前へ進めた。

## 2026-07-12: CP1 S3完了

- SQLite bootstrap、schema manifest検証、非対応DBの非破壊拒否を実装した。
- WAL / rollback journalを含む既存DBをsnapshot側で分類し、旧DB path aliasをopen前に拒否する契約を追加した。
- CP1の現在地をS4 Persistence Worker Lifecycle着手前へ進めた。

## 2026-07-12: CP1 S4完了

- Persistence Workerの世代付きprotocol、single FIFO、timeout / cancel、crash / shutdown lifecycleを実装した。
- 最大256 KiBのpayload chunk transfer経路とconstant-memoryのrequest replay拒否を追加した。
- CP1の現在地をS5 Repository Read Model着手前へ進めた。
- CP1 S5でscope付きbounded Repository read、opaque cursor、Message / payload chunk分離を実装し、現在地をS6着手前へ進めた。
- CP1 S6-Aでtyped write command、idempotency基盤、Session create / lifecycle transitionを実装し、現在地をS6-B着手前へ進めた。
- CP1 S6-B1で通常Run admissionを実装し、Message / Run / Attempt / Dispatch / Binding intent / IdempotencyRecordの一括commitを成立させた。
- CP1 S6-B2aでProvider Bindingのactive / ambiguous resolutionを実装し、ephemeral live ownershipをWorker memoryへ限定した。
- CP1 S6-B2bでDispatch共通Gate、送信intent、accepted / rejected / ambiguous resolutionを実装した。

## 2026-07-14: CP1完了

- S8 Integration Gateでpublic repository API、Main / Worker依存境界、clean install、Windows compiled smoke、SQLite sidecar cleanup、性能baselineを確認した。
- Node.js 24で全116 test、schema validator、runtime probe、lint、typecheck、build、formatを通し、materialなcorrectness / data-loss findingがないことを確認した。
- CP1を`完了`へ更新し、現在地をCP2 Application Service / CLI Control Plane着手前へ進めた。

## 2026-07-18: CP2 Session CLI control plane完了

- `ApplicationSessionOperations`の全Session操作を、version付きJSONと安定したexit codeを持つ`withmate session` CLIから実行可能にした。
- Sessionを主たる指定単位とし、Workspaceはcreate時に保存し、listの任意filterとして扱う契約へ統一した。
- caller supplied idempotency key、Application responseの明示的projection、Workerのstart / shutdown ownership、CLIからRepositoryへの迂回禁止を実行可能な契約で固定した。
- 全244 test、CLI process smoke、compiled persistence smoke、lint、typecheck、build、format、SQLite schema検証を通した。
- CP2全体は進行中のままとし、Run操作と後続control planeは別sliceで扱う。

### Accepted risks

| ID         | 発生条件と影響                                                                                                                            | 検知と復旧                                                                                             | 再判断条件                                                                                               |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------- |
| CP2-CLI-R1 | create時の`fs.stat`はOS I/O自体をcancelできないため、応答しないnetwork mountではoperation timeout後もprocessが残る可能性がある。          | 呼び出し元のprocess timeoutで検知し、対象processを終了できる。Session commit前でありデータ影響はない。 | network Workspaceをsupported scopeへ含める場合に、別processでの検証またはcancel可能な境界を検討する。    |
| CP2-CLI-R2 | base branchで作成されたpre-release schema v1 DBは、同じversion内のschema hash変更によりstartup時に拒否される可能性がある。                | startup failureとして検知できる。互換契約のない開発DBは再作成できる。                                  | schema v1の外部利用開始前、または既存DB保持がaccepted contractになった時点でmigration方針を決める。      |
| CP2-CLI-R3 | Windowsの通常path、extended-length path、UNC aliasはfile identityまで同一化しないため、同じdirectoryを別Workspaceとして扱う可能性がある。 | list結果のWorkspace path差異で検知でき、SessionはIDで引き続き操作できる。                              | file pickerや外部callerが複数のpath表現を渡す段階で、realpathまたはfile identityによる同一化を検討する。 |

## 2026-07-18: CP2 Session local-only delete CLI slice

- `withmate session delete`から、Session subtreeの主DB削除、Session Files削除、cleanup完了記録までをApplication Service経由で実行可能にした。
- `--confirm-local-only`を必須のvalueless CLI確認とし、Provider側のthreadまたはSessionを削除しないことをhelpへ明記した。
- primary commit後のcleanup失敗を、committed valueとcleanup tokenを保持する`partial_success`（exit code 10）として公開し、同一requestのexact retryでpending cleanupを再開する契約を追加した。
- busy subtreeの非変更、same-key/different-session競合、manifest page検証、Session Filesの固定root・symlink/junction拒否・missing時成功を実行可能なcontractで確認した。
- Session IDをRepository所有のincarnation identityへ変更し、通常Sessionとchild Sessionの両入口を同じallocatorへ統合した。削除後の通常create再送は別IDを発行し、旧delete再送が新incarnationを対象にしない。
- Session Files cleanupはRepository発行IDだけを内部入力として受け、検証済みrootへ作業directoryを固定したhelper processから相対削除する。manifest全体の検証が終わるまでfilesystem副作用を開始しない。
- CP2全体は進行中のままとする。Session Files orphan sweepと、process crashで残ったcleanupの自動探索・再開は後続sliceで扱う。

## 2026-07-19: CP2 Session delete review対応

- Session Files cleanupをDB所有application data directoryのidentityへ結び付け、親directory差し替え時にreplacement側を削除しない回帰contractを追加した。
- 通常writeとSession deletionのidempotency keyを共有claim registryへ統合し、cross-operation key再利用をRepositoryとSQLite schemaの双方向で拒否した。
- Session treeを4,096件に制限し、child admission、Repository delete、Application / CLI projection、schema manifestへ同じaggregate契約を展開した。
- subtree deleteをconnection-localなSQL worksetによるset-based削除へ変更し、関連ID群の全件hydrateを除去した。対象payload bytesと更新対象row数によるWAL見積りがdisk reserveを割る場合は、durable mutation前に`insufficient_disk_space`で拒否する。
- schema installは永続triggerを許可しつつ、transaction controlとTEMP schema objectをSQLite authorizerで拒否し、Worker connectionへschema artifactが残る経路を閉じた。

## 2026-07-19: CP2 Run observation control plane

- Provider非依存で成立する`ApplicationRunOperations`と`withmate run status|events|follow`を追加し、永続化済みRunの状態とbounded RunEvent pageをApplication Service経由で観測可能にした。
- followを1 invocation 1 responseのbounded long-pollとし、event、terminal closure、deadline、SIGINT abortを分離した。terminal status後のevent probe、opaque continuation、page / wait / poll上限を実行可能なcontractで固定した。
- Run statusとeventをallowlist projectionへ限定し、execution snapshot、Provider error code、内部ID、version、external side effect metadataをpublic出力から除外した。
- Run namespaceは既存`withmate-cli-v1`、exit code、stdout JSON、Workerのexactly-once shutdown契約へ追加した。CLI hard timeoutとSIGINTをbootstrap、operation、shutdownへ通し、parse / helpがruntimeを起動しないことをprocess smokeで確認した。
- production CLIには`start`、`retry`、active `cancel`を追加していない。Provider request / execution snapshotの構築、dispatch継続process、Provider interruptとterminal outcomeの相関を所有するruntimeが未確定である。
- Run observation sliceは完了したが、Provider runtime ownershipとmutation操作のcheckpoint帰属が未確定だったため、CP2全体は進行中のままとした。現在の帰属はD-006を参照する。

### Accepted risks

| ID         | 発生条件と影響                                                                                                                                                                                                                                                         | 検知と復旧                                                                                    | 再判断条件                                                                                                                                          |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| CP2-RUN-R1 | 将来live activity portをproductionへ接続した際、port rejectionまたはmalformed responseでは永続Run statusを取得済みでもApplication internal failureと`persistence.status='failed'`を返す。現行productionはdefault null portのため到達せず、データ破損や情報漏洩はない。 | structured failureとexit code 50で検知し、statusを再実行できる。永続Run stateは変更されない。 | CP3でlive activity portを接続する前に、補助表示を`null`へ縮退するか、persistence read済みのinternal failureを表せるenvelopeへ拡張するかを決定する。 |

## 2026-07-19: CP2 Run output control plane

- 長期判断は`docs/adr/012-run-output-control-plane.md`、public型は`src/shared/application-run-output-model.ts`、Applicationとfilesystem publicationの実行可能な契約は`test/application-run-output-service.test.ts`と`test/run-output-exporter.test.ts`を正本とする。
- `ApplicationRunOutputOperations`と`withmate run output-counts|outputs|output-preview|output-chunk|output-export`を追加し、永続化済みRun outputをApplication Service経由で扱えるようにした。
- Countsとitem pageをpayload BLOBから分離し、scope付きpoint readでpayload stateとredactionの組を検証する。TextとJSONは64 KiB previewと256 KiB chunkに制限し、binary本文はexplicit exportだけが消費する。
- ExportはCLI userが選んだabsolute destination grantをApplication side-effect boundaryへ渡す。Same-directory temporary fileへの逐次write、backpressure、lengthとSHA-256の照合、exclusive hard-link publishにより、既存destinationを上書きしない。
- Publicationは`published`、`not_published`、`unknown`を区別する。Timeout、cancel、helper response lossでpublish成否を確定できない場合は、destinationを確認してから再試行する。
- CLIは既存Run commandと同じJSON envelope、exit code、hard timeout、SIGINT、exactly-once shutdownを維持する。実DB smokeでcounts、list、preview、chunk、export、no-clobber、SQLite sidecar cleanupを確認した。
- Run output sliceは完了した。CP2にはSession Message timeline / content chunkとSession Run historyのApplication / CLI公開が残るため、CP2全体は進行中のままとする。

### Accepted risks

| ID                | 発生条件と影響                                                                                                                                                                                       | 検知と復旧                                                                                                                                      | 再判断条件                                                                                                                                               |
| ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CP2-RUN-OUTPUT-R1 | Destination directoryを同じcurrent OS userの敵対processがexport中に置換した場合、pathname raceを完全には防げない。通常のidentity不一致は検知するが、敵対processに対するsecurity boundaryにはしない。 | Identity不一致またはpublication不明として検知できる場合は`unknown`を返し、destinationを確認してから再試行する。                                 | Sharedまたはuntrusted directory、別principal、adversarial local processをsupported scopeへ含める場合は、native directory handle相対operationを検討する。 |
| CP2-RUN-OUTPUT-R2 | Process crashまたは強制終了では、publish前またはpublish後cleanup前のsame-directory temporary fileが残る可能性がある。Destinationは既存fileを上書きしないが、temporary fileがdiskを消費する。         | 停止後に`.withmate-output-*.tmp`を確認し、export processが動いていないことを確認して削除する。Destinationの有無と内容を確認してから再試行する。 | Long-lived runtime、automatic retry、定期maintenanceを導入する場合は、temporary file ownershipとsafe sweepを設計する。                                   |

## 2026-07-19: CP2 / CP3 / CP5 scope再整理

- CP2はProvider非依存の永続control planeとし、残作業をSession Message timeline / content chunkとSession Run historyのApplication / CLI公開へ絞った。
- 単一SessionのRun `start` / `retry` / active `cancel`、supplemental input、approval / elicitation responseは、Provider dispatchとlive runtimeを所有するCP3へ移した。
- child Session / Delegationの`start` / `follow-up` / `message` / `wait` / `collect` / `cancel` / `kill`はCP5へまとめた。作成済みchild Sessionへの追加指示もCP5の対象とする。
- CP6はCP3で確定する共通Run operation contractへ依存させ、Session Files cleanupとRun output export temporary fileのorphan sweep / crash recoveryはCP8へ移した。
- 具体的なCLI operation名は未確定であり、CP3とCP5でApplication contractと同時に決定する。現在のCP2実装とpublic contractは変更していない。

## 2026-07-19: CP2 Run output review対応

- Destination directoryの解決とidentity検証をexport helperへ移し、中断通知後も終了しないhelperを猶予時間後に強制終了するよう変更した。非協調helperを使うprocess-level testで、owner processがhard deadline内に終了することを確認した。
- `payload_unavailable`をdiscriminated unionへ変更し、`pending`と`retryable: true`、それ以外のreasonと`retryable: false`だけをApplication型とCLI型で許可する。CLI projectorはraw responseの矛盾した組を両方向とも拒否する。
- Node.js 24.18.0で全407 test、SQLite schema validator、typecheck、build、Run CLI process smokeを通した。既定shellのNode.js 22.22.1ではruntime guardが意図どおりfail-fastすることも確認した。

## 2026-07-20: CP2 Session Message control plane

- 初期Message content blockをexact keysの`{ type: "text", text: string }`へ固定し、dense array、10,000 block、UTF-8 4 MiB上限を共有validatorで検証する。normal Run admission、Run terminal、supplemental input、child startの全write siblingを同じ境界へ集約した。
- `ApplicationSessionMessageOperations`へ`messages`と`messageContentChunk`を追加した。authorization後にSessionからinternal workspace scopeを解決し、Repositoryでworkspace / Session / Messageの組を再検証する。public pageはordinal順のopaque cursor、inline 64 KiB、bounded omissionを持ち、大きい本文とRunOutput / Provider payloadをhydrateしない。
- `withmate session messages`と`withmate session message-content-chunk`を追加した。CLIはApplication responseを再検証し、chunkのactual bytesから`nextOffset`を確認してbase64へ一度だけ投影する。help / parse failureのruntime非起動、既存exit code、timeout / SIGINT、exactly-once shutdownを維持する。
- 既存`smoke-cli-run.mjs`を拡張し、実DB上のuser / assistant Message、small-limit cursor、inline / chunked分離、base64長、actual offsetでの本文再構成、wrong scopeの`not_found`、RunOutput非混入、SQLite sidecar cleanupを確認した。
- public contractは`src/shared/message-content.ts`、`src/shared/application-session-message-model.ts`、対応するtype / testを正本とする。ADR 006とD-006のhydrate分離、cursor、CLI ownershipの判断は変えていないため、新規ADRと設計文書の更新は行わない。
- Node.js 24.18.0で全435 testとSQLite schema validator、runtime guard、format、module boundary / lint、typecheck、buildを通した。Session CLI、Run / Message CLI、compiled persistenceのprocess smokeもGreenで、SQLite sidecarが残らないことを確認した。
- Session Message sliceは完了した。CP2全体はSession Run historyと統合Gateが残るため、引き続き進行中とする。

## 2026-07-20: CP2 Session Run history control plane

- Repository readへSession / Workspace scope付きの`runs.page`を追加した。Run headerだけを`runs_session_ordinal_uq`によるordinal keysetで1 statement取得し、default 50 / maximum 100、opaque scope cursor、192 KiB response budget、ordinal付きomissionを適用する。execution snapshot、Message本文、RunEvent、RunOutput、RunAttempt、RunDispatch、ProviderBindingは取得しない。
- `ApplicationSessionRunOperations.runs`を追加した。`session_runs` authorization後にSessionからinternal Workspace scopeを解決し、Repository境界で再検証する。Run historyと既存Run statusは永続phase / failure / cancellation / timestamp projectionを共有し、completedでfinal assistant Messageがない組を含むphase-specific unionへ投影する。
- `withmate session runs --session-id <id> [--cursor <cursor>] [--limit <1..100>] [--timeout-ms <ms>]`を追加した。既存Session CLIのversion付きJSON、exit code、timeout / SIGINT、exactly-once shutdownを維持し、Application responseをstrict allowlistで再検証する。
- 実DB process smokeで、3件のRunのordinal page、small-limit cursor、別Sessionへのcursor流用拒否、completedかつfinal assistant Messageなし、historyとstatusのtimestamp一致、historyで得たRun IDからevents / outputへの遷移、internal field非露出、help時のruntime非起動、SQLite sidecar cleanupを確認した。
- Node.js 24.18.0で全459 testとSQLite schema validator、runtime guard、format、module boundary / lint、typecheck、buildを通した。Session CLI、Run / Message / Session Run history CLI、compiled persistenceのprocess smokeもGreenだった。
- public contractは`src/shared/application-session-run-model.ts`、共有phase projection、対応するtype / testを正本とする。既存ADR 006 / 011とD-006のauthorization、projection ownership、CP2 / CP3 / CP5分離を変更していないため、新規ADRとdesign文書の更新は行わない。
- Session Run history sliceは完了した。CP2全体は次の`test/cp02-control-plane-gate`による統合Gateが残るため、引き続き進行中とする。

## 2026-07-20: CP2 Control Plane統合Gate完了

- Session createからMessage timeline、Session Run history、Run status / events / follow、output counts / list / preview / chunk / exportまでを、Providerを起動せずproduction CLIで辿った。large Messageとtext / JSON outputはactual byte offsetで再構成し、binary outputはexplicit exportだけで公開した。
- owner / scope、opaque cursor、192 KiB page budget、64 KiB inline / preview、256 KiB chunk、4 MiB Message上限、16 / 64 MiB output quota、payload非hydrate、strict allowlist projectionを、Application、Repository、CLI contractと実DB smokeへ対応付けた。
- exact retry、same-key / different fingerprintまたはoperationのconflict、Session incarnation、commit応答喪失後のsame identity replay、`effect: "unknown"`と`reconciliation: "exact_request_required"`の組を既存Application、Repository、Worker contractで確認した。同じfailure timingを重複するtestは追加していない。
- 独立Gate evidence reviewで、Session renameの構造的Repository write bypassをmodule-boundary checkerが検出できない`blocking` findingを確認した。Session writeの5操作を同じ禁止capability集合とnegative fixture health checkへ揃え、修正前のRed、修正後のGreen、targeted re-review findingなしで閉じた。
- Node.js 24.18.0でruntime guard、format、lint、typecheck、全459 testとSQLite schema validator、buildを通した。build後のSession CLI、Run / Message / Session Run history CLI、compiled persistenceのprocess smokeもGreenで、SQLite sidecarとSession Files cleanup artifactが残らないことを確認した。
- 既存accepted riskのCP2-CLI-R1からR3、CP2-RUN-R1、CP2-RUN-OUTPUT-R1からR2は、発生条件、影響、検知、復旧、再判断条件が引き続き妥当である。Gateではnetwork Workspace、path aliasのfile identity化、live activity port、adversarial directory、schema migration、temporary file orphan sweepをsupported scopeへ追加していない。新しいrisk-candidateと未実行のGate validationはない。
- 既存ADR 003 / 006 / 011 / 012とD-006の責務、failure、checkpoint分離を変更していないため、新規ADRとdesign文書の更新は不要と判断した。CP2を`完了`とし、現在地をCP3着手前へ進めた。CP3は`未着手`のままで、Q-11は回答していない。

## 2026-07-20: CP3 Codex runtime contract確定

- `codex-cli 0.144.6`とNode.js 24.18.0を使用し、stable / experimental schemaをrepository外へ生成した。stable 267 file、experimental 337 fileで、`turn/steer`、`turn/interrupt`、agentMessage phaseはstable schemaに存在した。
- 隔離した一時workspace、read-only sandbox、approval=neverで`runtime-contract-probe.mjs`を2回実行した。CAS-009は空interrupt response、Thread idle、`turn/completed(interrupted)`の順、CAS-010はTurn不一致とactive Turn不在の拒否、同一Turnへのsteer受理とuser Message履歴反映、CAS-016は`commentary` 1件と`final_answer` 1件を両回で確認した。
- CAS-017はWindowsでCodex daemon lifecycleが非対応のため`blocked`とした。既存daemonのinstall、start、stop、restart、設定変更は行っていない。`0.145.0`のCLI helpにはexperimental WebSocket endpointが存在するが、初期transportには採用していない。
- ADR 013で、CLIやWindowから独立した長寿命WithMate runtime hostを1 current OS user / 1 application data rootのownerとした。runtime hostがPersistence Worker、stdio App Server child、live Run、draft、interactionを所有し、operational CLI / GUIはOS-local IPC clientとする。
- public operation名を`withmate run start`、`withmate run retry`、`withmate run send-input`、`withmate run cancel`に確定した。`pending`だけを安全な自動送信候補とし、`dispatching` / `ambiguous`を自動再送せず、Provider履歴から欠落Message / RunEvent / draftを推測生成しない。
- Q-11を確認済みとし、CP3を`進行中`へ更新した。このsliceはproduction source、schema、CLI commandを実装していない。次のproduction branchは`feat/cp03-runtime-host`とし、runtime host、single-owner起動、local IPC、既存operational CLI compositionの移行から開始する。

### Accepted risks

| ID             | 発生条件と影響                                                                                                                                                                                                                              | 検知と復旧                                                     | 再判断条件                                                                                                                                |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| CP3-RUNTIME-R1 | CAS-010の履歴反映を確認するためpersistent Threadを作成する。repository外workspaceの削除後も、syntheticなThreadが設定済みCodex profileへ残る可能性がある。既存Thread、repository data、secretは変更せず、Thread IDと本文を証跡へ出力しない。 | CodexのThread一覧で検知し、不要なら対象Threadをarchiveできる。 | 隔離したProvider stateで認証を安全に利用できる手段、または作成Threadを確実に削除できるstable APIが利用可能になった時点でprobeを更新する。 |

## 2026-07-20: CP3 Codex App Server stdio transport

- 新しいユーザー指示に基づき、runtime hostより先にCodex App Server transportを独立実装した。ADR 013のowner判断は変更せず、transport instanceとchild processの最終ownerは後続runtime hostのままとする。runtime host、local IPC、Application composition、Provider固有のThread / Turn / item変換は実装していない。
- `src/main/providers/codex/`へ、`codex app-server --stdio`のprocess ownership、incremental UTF-8 JSONL framing、strict outer envelope validation、request correlation、server request / notification routing、`initialize` / `initialized` handshake、deadline / abort、bounded closeを配置した。後続hostとAdapterが利用するsurfaceは同directoryの`index.ts`へ限定し、framer、wire writer、protocol sessionは内部実装のままとした。
- 公開failureは、送信前の`request_not_sent`、送信後の`response_unknown`、remote error codeだけを持つ`remote_error`、connection / protocolの`connection_failure`へ分離した。active writeとqueued writeの終了順を分け、closeまたはstdio failureでもactiveはoutcome unknown、未送信queueはpre-send rejectionとして一度だけ収束する。transportによる自動retryは行わない。
- default resource limitは、protocol line 1 MiB、pending request 128件、retired unsent client request ID 4,096件、outstanding server request 128件かつIDのUTF-8合計256 KiB、queued event 128件、queued write 2 MiB、stderr retention 64 KiBとした。settled server request IDは保持せず、長寿命connectionのlifetime capにしない。未送信IDの相関記録が上限へ達したconnectionは、安全なsent / unsent判定を失う前にprotocol failureへ収束する。stderrは件数とbyte数だけを保持し、raw text、secret、account情報、private pathをdiagnosticへ投影しない。
- framing、correlation / handshake、process lifecycle、integrated boundaryの各sliceをtargeted contract testと独立reviewで閉じた。reviewで確認したduplicate JSON member、handshake terminal error、server request ID lifetime、cleanup rejection、active / queued write outcome、server responseのpre-send retry、終了後のowned process参照保持をsourceとexecutable contractへ展開した。
- 2026-07-21のcomplete-diff reviewで、process tree残留、受信済みevent消失、stdout drain前の終了判定、未送信queueの誤分類、initialization sequenceのdeadline / abort漏れ、spawn前validation漏れを確認して修正した。root先行exit、queued request cancellation、clientInfoとresource limitの複合値を対象としたclosure reviewとtargeted re-reviewを行った。
- 後続reviewで、未送信request IDへのresponse受理、root終了後のWindows process tree再探索、未処理stderr error、JSON-RPC 2.0 responseのversion欠落、settled server request IDのlifetime保持を追加確認し、同じcontract familyへ展開して修正した。WindowsはADR 014に従い、起動待ちsupervisorを割当後にCodexをspawnするJob Object ownershipへ変更した。PID再探索は廃止し、中間launcher終了後のdescendant回収と無関係processの非干渉をprocess testで確認した。
- 独立closure reviewで、timeout / abortまたはpre-send write rejectionによりsettle済みとなった未送信IDへのresponseがlate anomalyへ誤分類される兄弟sequenceを確認した。未送信IDをboundedにretireし、response受理または相関上限到達をprotocol failureへ収束させた。response先行、settle先行、write rejection、resource cap、close / fail cleanupをtargeted re-reviewし、未解決のblocking / risk-candidate findingはない。
- 最終reviewで、sparse executable argumentsの検証迂回とNode timer上限超過値の1ms丸めを確認した。argumentsはdense own indexとread-once snapshotを外部process起動前に検証し、未指定時だけdefaultを適用する。startup、close、handshake、requestのtimeoutは1から2,147,483,647msへ統一し、wall-clock巻き戻り後のremaining durationも同じ範囲へclampする。changing accessor、options getter、null、exact maximum、maximum超過をcontract testへ追加し、targeted re-reviewで未解決のblocking、risk-candidate、validation gapがないことを確認した。
- Node.js 24.18.0でruntime guard、format、module boundary / lint、typecheck、build、SQLite schema validator、Codex transport targeted 69 test、Koffi経由のJob Object作成、実Codex 0.144.6へのinitialize-only smokeを通した。全532 testもGreenで、runtime dependencyのauditは0 vulnerabilities、新しいaccepted riskはない。

## 2026-07-22: CP3 Codex App Server Adapter

- `codex-cli 0.145.0`のstable schemaをbaselineとし、model catalog取得、Thread開始・再開・読取、Turn開始・steer・interruptを型付きAdapter operationとして実装した。approvalは`never`に固定し、request送信前、送信済み、Provider拒否、response不整合のeffect certaintyを分離して自動retryを行わない。
- responseとnotificationを単一のThread / Turn lifecycleへ収束し、completed / failed / interruptedを単調なterminalとして扱う。duplicate、out-of-order、遅延response、terminal先行をbounded stateとtombstoneで処理し、別Threadまたは別Turnのstateへ混入させない。
- agent Messageの`commentary`、`final_answer`、phase unknown fallback、plan、reasoning、operation、未対応itemをbounded outputへ変換した。Provider terminal outcome、final assistant Message、content failureを分離し、operation raw payload、private path、secret、大きいunknown payloadをpublic eventまたはdiagnosticへ投影しない。
- connection / Thread / Turn / item / event queue / text / diagnosticのaggregate limitをprospectiveに検証し、known-invalid event、unsupported server request、protocol anomaly、resource超過を診断後のconnection failureへ収束した。closeはidempotentで、pending waiter、lifecycle、item、retained textを解放し、event pump自身をawaitしない。
- production `CodexAppServerTransport`からAdapterまでのprocess-level contractを追加し、initialize後のstable model catalogを実process境界で取得した。runtime host、Application / Persistence composition、Run dispatch、approval / elicitation interaction、CLI operationはこのsliceに含めず、CP3は進行中のままとした。
- Node.js 24.18.0でAdapter targeted 101 test、全622 TypeScript test、runtime guard、format、module boundary / lint、typecheck、buildを通した。`npm test` wrapperは全620 TypeScript testがGreenになった後、Windows `py -3` launcherがインストール済みPythonを検出せずexit 1となることを確認した。追加contractを含む全622 TypeScript testを`tsx`から再実行し、同じSQLite schema validatorをインストール済みPython 3.12から直接実行してGreenを確認した。全体spec実行では既存export timeout testが一度だけtiming差で失敗したが、対象testの単独再実行と後続の全622 test再実行はGreenだった。
- complete diffの独立reviewで、Turn output aggregate、stable notification分類、stable schemaのoptional field、model / reasoning tuple、IDなしunknown notification、terminal先行tombstoneの6 familyをblockingと判定した。closureではstable schemaのaudio / webSearch / MCP exactness、Turn response / admission、pending owner、catalog load中のclose / failureへ同じinvariantを展開して修正し、targeted re-reviewで未解決のblockingとrisk-candidateがないことを確認した。
- fresh-context closure reviewでは、active Turnに相関しないusage / error、未確定ThreadへのTurn送信、steer / interruptのactive tuple、modelのtext modality、Thread mutationのpending aggregate、notification / response race、steer acknowledgement、stable reasoning effort、design上のoperation scope、履歴内の重複Turn / item IDへ反例を展開した。response-confirmed Threadだけをadmissionに使い、modelの明示選択と既存history継続を分離し、曖昧なThread / Turn mutation ownerをcloseまで保持するexecutable contractへ揃えた。重複IDは共有decoderで拒否し、readは`invalid_response`、送信済みmutationは`ambiguous`へ収束させた。
- 既存ADR 013と`docs/design/codex-app-server-adapter-contract.md`がowner、failure、mappingの判断理由を所有しており、新しい長期判断は追加していない。修正後のtargeted re-reviewで未解決のblockingとrisk-candidateがないことを確認し、新しいaccepted riskはない。
- `codex-cli 0.145.0`のnotification envelopeにある`emittedAtMs`をexact validationへ追加し、production transportのprocess contractで未知notificationを受信しても接続を維持することを確認した。未知notificationのThread / Turn / item相関tupleはAdapter内部のowner照合とdedupeへ投影するが、publicまたは永続化する`provider_metadata` / diagnostic payloadには含めない。
- active Threadのresumeでは唯一の`inProgress` Turnをcurrent active tupleとして復元し、二重Turn開始を拒否したうえでsteer / interruptを既存Turnへ相関する。Thread statusとcurrent Turnが矛盾するresponse、またはnotificationとresponseのID競合では候補を選ばず`ambiguous`へ収束させる。
- Thread / Turn開始notificationまたはinterruptのterminal notificationで副作用を観測した後に対応requestが`remote_error`となった場合は、`effect: none`へ戻さず`ambiguous`へ収束させる。Provider未送信を証明できる`request_not_sent`は、無関係なnotification観測があっても`effect: none`を維持する。受理済みTurnのmodel overrideはThreadのcurrent modelを更新し、後続Turnのcapability検証へ反映する。
- 追加の独立reviewでは、Thread開始、Turn開始、interruptのnotification-first矛盾とID競合を同じinvariant familyへ展開した。修正後のtargeted re-review 44 testを通し、未解決のblockingとrisk-candidateはない。実App Serverにおけるsteerのnotification先行順序とactive Thread resume時の既存item再配信は未実測であり、`docs/design/codex-app-server-adapter-contract.md`のruntime検証Gateとして残した。
- fresh-context complete-diff closure reviewでは、notification先行model overrideの未送信rollback、terminal前後をまたぐnotification / response Turn ID不一致、current Turnを相関できない`active` Threadの送信Gateをblockingと判定した。暫定modelはpending owner identityで管理し、`request_not_sent`では同じownerだけを元のcurrent modelへ戻す。Turn IDはlifecycle受理前に観測し、terminal後もresponseと直接照合する。`active`のままcurrent TurnがないThreadへは新しいTurnを送らない。
- targeted closure reviewで、古い`request_not_sent`と新しい同一model ownerの競合、terminalから遅延した`turn/started`後の別response IDへ同じinvariantを展開した。model値が同じ場合も新ownerへtagを移し、遅延notificationのIDはlifecycleで拒否されてもpending ownerへ保持する。修正後のtargeted reviewで未解決のblockingとrisk-candidateはなく、新しいaccepted riskはない。
- 追加reviewで、steer入力の配送相関、event queue内のterminal保持、Thread status別のTurn開始Gate、Windows Workspace identity比較をblockingと判定した。`turn/steer`へ`clientUserMessageId`を付与して`userMessage.clientId`と相関し、配送観測後の失敗を`effect: none`へ戻さない。accepted response後もownerをmatching notificationまたはterminalまで保持し、別Turnでの観測は両Threadのmutation admissionを`ambiguous`へ閉じる。
- event queueは、terminalを追加するときも既存terminalを削除せず、削除可能なnon-terminal eventがなければconnection failureへ収束する。Turn開始はcurrent Turn不在かつThread statusが`idle`の場合だけ許可する。Thread開始・resume responseの`cwd`はdisplay文字列ではなく共有Workspace identity keyで相関する。
- 修正後はAdapter / Wire targeted 107 test、全628 TypeScript test、runtime guard、format、module boundary / lint、typecheck、build、SQLite schema validator、diff checkを通した。全体testでは既存Application deadline testが一度だけtiming差で失敗したが、単独再実行と後続の全628 testはGreenだった。`npm test` wrapperのWindows `py -3` launcher問題は既知のvalidation gapで、同じschema validatorをインストール済みPython 3.12から直接実行してGreenを確認した。
- 独立targeted reviewで、accepted steer response直後にownerを破棄してresponse-firstの後続notificationを相関できないblockingを確認し、settled ownerの保持とterminal / failure / closeでのbounded cleanupへ修正した。targeted re-reviewでは未解決のblockingとrisk-candidateはなく、新しいaccepted riskとADR判断は追加していない。
- `codex-cli 0.145.0`からstable JSON Schemaを再生成し、modelの`inputModalities`とagentMessageのnullableな`phase`が省略可能で、それぞれ`["text", "image"]`と`null`の既定値を持つことを確認した。decoderはfield欠落時だけschema既定値を適用し、明示された不正値はresponse境界で拒否する。
- `thread/resume`のpending ownerを、同じThread IDの`thread/started` notificationと相関する。notification受理後の`remote_error`は`ambiguous / effect: unknown`へ収束させ、`request_not_sent`は`effect: none`を維持してownerを解放する。独立targeted reviewで確認した、resume#1の`request_not_sent`後にresume#2のnotificationがlifecycle上のduplicateとなる反例も、mutation evidenceをduplicate判定前に記録して閉じた。
- 修正後はAdapter / Wire targeted 115 test、全636 TypeScript test、runtime guard、format、module boundary / lint、typecheck、build、SQLite schema validator、Markdown format、diff checkを通した。独立targeted re-reviewで未解決のblockingとrisk-candidateがないことを確認した。既存の設計契約を厳密化した変更であり、新しいADRとaccepted riskは追加していない。`npm test` wrapperの既知のWindows `py -3` launcher問題は変わらないため、TypeScript testとschema validatorを個別に実行した。
- 追加reviewで、Thread notification / responseのWorkspace・persistence相関、signed int32の`commandExecution.exitCode`、最新requestへ置換されるtoken usage `last`、interruptと全terminal statusの競合をblockingと判定した。Thread相関tupleへWorkspace identityとephemeral modeを含め、`exitCode`をnullable signed int32として検証し、usageは`total`だけを前回値と単調比較する。pending interruptの対象Turnで`completed`、`failed`、`interrupted`のいずれかを観測した後の`remote_error`は`ambiguous`へ収束させる。
- response-firstのThread identity不一致後は、そのThreadをquarantineし、非同期capability validation後かつProvider send直前にも再確認する。`turn/start`とmodel指定の`thread/resume`の両入口で、競合中のmutationを未送信へ収束させる。
- 修正後はAdapter / Wire targeted 122 test、全643 TypeScript test、runtime guard、format、module boundary / lint、typecheck、build、SQLite schema validator、diff checkを通した。今回は`npm test` wrapperからSQLite validatorまでGreenとなり、直前の作業で記録したWindows `py -3` validation gapは現在の環境では再現しなかった。独立targeted re-reviewで未解決のblockingとrisk-candidateがないことを確認した。既存のAdapter契約を厳密化した変更であり、新しいADRとaccepted riskは追加していない。
- 追加reviewで、`turn/started`を伴わないterminal-first Turnが`remote_error`後に`effect: none`へ戻る問題と、`thread/resume`のeffect unknown ownerを保持しながら同じThreadへTurn mutationを送信できる問題をblockingと判定した。pending `turn/start`は未観測のTurn IDを持つterminalも副作用証拠として記録し、以前に受理済みのterminal duplicateは新しいrequestへ相関しない。同じThreadに未解決のThread mutation ownerがある間は、`turn/start`、`turn/steer`、`turn/interrupt`を送信しない。
- 修正後はAdapter / Wire targeted 128 test、全649 TypeScript test、Node.js 24.18.0のruntime guard、format、module boundary / lint、typecheck、build、SQLite schema validator、Markdown format、diff checkを通した。独立targeted reviewで未解決のblockingとrisk-candidateがないことを確認した。
- 追加reviewで、response確定済みThreadの遅延`thread/started`を後続のpending `thread/start`へ誤帰属する問題と、`turn/steer`の`response_unknown`後に配送相関ownerを破棄する問題をblockingと判定した。response-confirmed Thread IDへの通知は既知Threadへ収束させ、別のpending startへ副作用証拠として渡さない。ambiguous steerのclient ID ownerはmatching `userMessage`または対象Turnのterminalまで保持し、遅延したtuple競合を検出する。terminalがresponseより先に到着した場合は、response settle時にownerを解放する。
- 修正後はAdapter / Wire targeted 132 test、全653 TypeScript test、Node.js 24.18.0のruntime guard、format、module boundary / lint、typecheck、build、SQLite schema validator、Markdown format、diff checkを通した。独立targeted reviewでは、ambiguous steerのmatching deliveryによるowner解放も専用testへ追加し、未解決のblocking、risk-candidate、validation gapがないことを確認した。
- test contract reviewで、public summaryとAdapter close errorの英語文言を完全一致していたassertをblockingと判定した。diagnostic code、分類、相関tuple、completion state、payload、redactionは直接検証したまま、summaryは非空、UTF-8 byte上限、既知の非公開値を含まないことを共有assertionで検証する。closeは文言ではなくrejectする契約だけを固定する。
- request optionsのsnapshot testは、別identity、freeze、呼出直後に元objectを変更しても送信済みtimeoutとsignalが変化しないことを検証する。既存のsnapshot境界がこのcontractを満たしたため、production sourceは変更していない。修正後はAdapter / Wire targeted 132 test、全653 TypeScript testとSQLite schema validator、Node.js 24.18.0のruntime guard、format、module boundary / lint、typecheck、build、diff checkを通した。
- 独立targeted reviewで、unknown notification、warning、error、late error、known-invalid payload、通常methodのunsupported server requestにsummary安全性と構造化fieldの兄弟assertionが不足しているblockingを確認した。同じ共有assertionへ揃え、code、method、correlation、willRetry、redactionを各経路で直接検証する。最終差分でAdapter / Wire targeted 132 test、全653 TypeScript testとSQLite schema validatorを再実行し、targeted re-reviewで未解決のblocking、risk-candidate、validation gapがないことを確認した。

## 2026-07-26: CP3 production runtime hostとoperational CLI移行

- 1 current OS principal / 1 canonical application data rootをowner tupleとする長寿命runtime hostを実装した。Windows named pipeまたはUnix domain socketのfail-closedなlocal authorization、single-owner claim、世代付きhandshakeを確立してから、1世代のPersistence Worker、Session Files、既存Application Serviceをhost内で構成する。
- version付きinternal JSONL protocol、exact validator、binary codec、operation allowlist、per-connection / host aggregate resource limitを追加した。client authorizationは受け付けず、hostがlocal authorizationを注入する。IPC request IDはconnection correlationだけに使い、domain idempotency keyを置き換えず、response loss後の自動retryを行わない。
- 既存21 operational operationをruntime IPC clientへ移行し、production CLIからone-shot Application / Persistence compositionへ到達する経路を削除した。help、version、parse failure、confirmation不足はruntimeを起動せず、CLI lifecycleのshutdownはclient connectionだけを閉じる。
- compiled process smokeで、同時host起動が1 ownerへ収束すること、別CLI processが同じgenerationを共有すること、forceful crash後に新generationが同じDBのSessionを読めること、graceful stop後にWorker checkpoint、endpoint解放、SQLite sidecar cleanupが完了することを確認した。test cleanupは起動時に保持したchild handleだけを対象とし、production stop commandは追加していない。
- compiled Application compositionのimport graphでProvider起動経路がないこと、module-boundary checkでproduction CLIのdirect Application / Persistence fallbackがないことを確認した。Run mutation、Codex Adapterのproduction接続、Provider dispatch、startup reconciliation、schema変更は行っていない。
- Node.js 24.18.0でruntime targeted test、全716 testとSQLite schema validator、runtime guard、format、module boundary / lint、typecheck、build、runtime host / Session CLI / Run CLI / compiled persistenceの各process smokeを通した。Windowsのnamed pipe、ACL、同時起動、detached host、crash recovery、graceful cleanupは実processで確認した。Unix socketの実process permission検証はWindows環境では未実行で、platform-independentなUnix metadata / error classification contractだけを確認した。absent endpointからのcompiled CLI bootstrapは、unit bootstrap、production detached spawn、compiled CLI接続へ分解して検証しており、単一process smokeとしては未実行である。
- 各sliceの独立targeted reviewを実施した。Slice 6では、同時spawnの部分成功、controlled shutdown IPC送信失敗、CLI timeoutの各経路でowned childのexit確認前にtemporary artifact cleanupへ進み得るblockingを同じfailure timing familyとして修正した。修正後のprocess smokeはpartial spawn、graceful stop failure、IPC send failure、CLI hangを決定的に再現し、回収不能時はartifactを削除せずcleanup failureを表面化する契約を確認した。targeted re-reviewで未解決のblockingとrisk-candidateはない。
- initial complete-diff reviewでは、durable writeのresponse lossがRuntime Application proxyから例外として漏れ、CLIで`effect: unknown`と`exact_request_required`を失うblockingを確認した。transport execution certaintyをApplication failureへ写像し、durable write、read、exportとpublic CLI projectionの回帰contractを追加した。
- fresh-context complete-diff closure reviewでは、shutdown deadline後に未収束のdurable requestを残してowner claimを解放できるblockingを確認した。caller boundaryとcanonical ownership finalizationを分離し、durable Application operation、Application shutdown、listener / connection cleanupの完了後だけclaimを解放する。slow response writeはApplication settlementと分離し、clientがresponseを読まなくてもlistener closeで中断してownership cleanupを進める。修正前Redと修正後Greenを確認し、targeted closure reviewで未解決のblockingとrisk-candidateがないことを確認した。
- 後続reviewでは、Session createの同一Workspaceを表すrequest / resultが文字列表現の差でIPC response validationに拒否されるblockingと、Persistence Workerのstartup failure時にWorker exit確認前にowner claimを解放できるblockingを確認した。Workspaceはhost path identityで相関し、別identityと非canonicalなresultは拒否する。Worker startupのtimeout、abort、`startupFailed`、crashは同じexit-confirmed failure境界へ収束させ、終了を遅延させたWorkerが残る間は競合claimを`busy`に保つ。修正前Redと修正後Greenを確認し、targeted closure reviewで未解決のblockingとrisk-candidateがないことを確認した。
- Windows named pipeの追加reviewでは、clientがserverのowner / DACL確認前に`SecurityImpersonation`を許可するblockingと、Application startup中に切断したreadiness clientをfatal accept errorとしてhost全体を終了するblockingを確認した。client SQOSをidentityとprivilegeの照会だけを許す`SecurityIdentification`へ制限し、server側のcurrent principal照合は維持した。accept前に切断済みとなったpipe instanceはconnection-localな終了として閉じ、first-instance排他を再確認した新しいinstanceでacceptを継続する。独立reviewで確認したpre-aborted acceptの見落としも、待受instanceを消費する前のabort再確認へ揃えた。修正前Redと修正後Green、後続handshake、Windows ACL / capacity / close / abort / first-instance排他を確認した。
- 最終reviewでは、ready後のPersistence Worker crashでruntime hostが壊れた世代のendpointとclaimを保持し続けるblockingと、Unix listenerがaccept前に切断したconnectionを待機queueへ保持するrisk-candidateを確認した。Worker clientはfatal stateへの遷移時にrequestを失敗させつつ、Worker exit確認後だけowner Applicationへfatal lifecycleを通知する。fatal stateでのshutdownもexit前にはsettleさせない。runtime hostは同じcanonical finalizationへ入り、Application shutdown結果にかかわらずlistenerとclaimを解放して、新しいclient / generationを開始可能にする。Unix listenerはactive connectionとaccept待ちqueueを同じregistryで所有し、close時に両aggregateから参照を除去する。終了を遅延させたWorker、競合claim、ready後のhost finalization、pre-accept disconnectの回帰contractを追加し、未解決のblockingとaccepted riskはない。
- endpoint ownershipとpublic operation boundaryは既存ADR 013が所有する。wire fieldとvalidationはtype / executable contractを正本とし、新しいADR、schema変更、accepted riskは追加していない。

## 2026-07-27: CP3 Run start / retryとCodex production dispatch

- `ApplicationRunOperations`へRun start / retryを追加した。startはmodel、reasoning effort、sandboxの明示を必須とし、retryはsource Runのexecution snapshotを既定値として、指定された項目だけを上書きする。Provider、Workspace、approval、Characterはcallerから受け取らない。
- RepositoryはMessage、Run、Attempt、Binding identityを発行し、admission、capacity、idempotency fingerprintを一つのtransactionで確定する。exact replayは保存済みphaseを返し、未確定または別fingerprintのrequestを新しいProvider mutationとして実行しない。
- runtime host内でCodex Provider processを遅延起動し、Binding作成またはresume、Dispatch、output、final Message、terminal eventを一つのowner tupleへ相関した。Provider process failureは受理済みRunを`interrupted`へ収束させ、runtime hostと別Sessionの永続操作を継続する。
- internal IPCとCLIへ`run start` / `run retry`を追加した。client disconnectやresponse lossはdurable writeをcancelせず、CLIはeffect unknownを自動再送しない。再送には同じdomain idempotency keyと同じrequestが必要である。
- deterministic fake Codex processを使うcompiled process smokeを追加した。隔離したapplication data rootで、通常start、host再起動後のterminal exact replay、retry、response loss後のexact replay、同時Session、client disconnect後のRun継続、Provider process failure、graceful shutdownを確認した。production profile、既存DB、実Codex executableは使用していない。
- public capacity errorから内部Provider identityを除外し、fresh admissionは`queued`だけを返し、exact replayだけが保存済みphaseを返せる契約へ揃えた。Application、runtime IPC、CLIの各境界で不正なreplay stateとphase tupleを拒否する。
- Codex processの`initialize`で得た`userAgent`をschema baselineと照合し、不明または非対応versionではAdapter operation前に接続を閉じる。fake process smokeで非対応versionが`model/list`、Thread、Turnへ到達せず、後続の対応versionで新しいgenerationを開始できることを確認した。
- Dispatchの受理が`ambiguous`な間は自動再送せず、同じProvider executionを照合できるgenerationを失った時点で、外部副作用の不確実性とDispatch stateを保持したままRunを`interrupted`へ収束させる。terminal response lossでは同じfrozen commandだけを再試行し、Sessionとcapacityのownerはterminal commit後に解放する。
- modelとreasoning effortの組をThread作成・再開前にcapability検証し、未対応の組でProvider Threadを変化させない。Session projectionのWorkspace key / canonical pathをApplicationで再照合し、RepositoryとAdapterは同じ32,768文字境界を使う。identifier向けの文字数またはUTF-8 byte上限をWorkspace pathへ誤適用しない。
- Application / RepositoryのMessage上限であるUTF-8 JSON 4 MiB / 10,000 blockをCodex Adapterの`turn/start` / `turn/steer`でも維持する。Sessionが受理するworkspace path / additional directories、Message、JSON escape、App Serverの`text_elements`とwire envelopeの複合上限を共有定数から導出し、execution snapshot、Provider request、stdio line、queued writeの各境界で合法な組を狭めない。runtime IPC / CLIのinline上限は64 KiB / 4,096 blockのままである。
- durable admission後の一時的なread失敗と、Provider Thread受理後のBinding resolution不明では、runtime hostが同じownerとfrozen commandを保持してread-backを再試行する。shutdownも未完了ownerを一度ずつ再駆動し、一時障害から回復した後の再shutdownでRunを終端化できる。event consumer失敗時はProvider connectionをcloseしてからgenerationを解放し、旧generationの遅延eventを遮断する。
- Dispatch beginが`queue_full`または`worker_closing`のretryableな`effect: none`で失敗した場合も、Providerへ送信せず、同じfrozen commandとRun ownerを保持する。in-flight workと重なったexact replayは、work完了後に新しく登録されたpending Dispatchだけを再駆動する。shutdownはin-flight workのsettlement後にもDispatch ownerをflushし、transientなPersistence Worker failureによってadmission済みRunを`queued`のまま放置しない。
- RunOutput writeの効果が不明な間は後続outputをcommitせず、受信順と永続ordinalを維持する。eventまたはoutput上限を超えたRunには`runtime_resource_limit`のterminal diagnosticを残し、保存欠落を正常成功として隠さない。
- Dispatch begin、Provider送信前のrejected resolution、accepted / ambiguous resolution、RunOutput、terminal commandの永続化が一時失敗した場合は、Provider eventの再受信やpublic requestの再実行へ依存せず、runtime hostがfrozen commandのpersistence-only retryを自動継続する。shutdown中にcontext readが完了したRunも未送信terminalへ収束させ、durable ownerを取り残さない。
- Provider connectionはclose完了後だけgenerationを解放する。Adapter、transport、process group、native handleは失敗したclose結果を成功扱いせず、同じownerを保持して再試行する。Windows Job Objectの設定、process割当て、一時process handle解放でpartial acquisitionとなった場合もcleanup ownerをtransportへ引き渡し、同じJob / handleをclose成功まで再試行する。Adapter公開前のstartup cleanupもfactoryが保持し、close成功までsuccessor processを開始しない。shutdown中のclose失敗はpendingとしてruntime hostのclaimを保持する。ambiguousなThread作成・再開もRun収束後にgenerationを退役させ、Adapter内のmutation reservationを次のRunへ持ち越さない。
- retry snapshotへmodelの`explicit` / `inherited`を記録する。継承時はsource Runのmodel値とprovenanceをAdapterまで渡し、同じThreadの後続Runがmodelを変更していてもsource Runの値へ戻す。hidden historyは新規選択ではないため`selectable`を要求しないが、catalog上の存在、入力modality、reasoning effortは検証する。Workerは通常startのexplicit model、retryのsource Run model、Session Workspace、Message / snapshot / Provider requestの複合不変条件をadmission transactionで再検証する。
- fresh-context complete-diff reviewでは、Provider送信前の決定的失敗を`failed`としてterminal化するRuntimeと、その組を拒否するWorkerの不一致を確認した。`binding_creation_not_sent`と`dispatch_not_sent`は`failed`、`canceled`、`interrupted`を原因に応じて受理し、Binding、Dispatch、Run、Attempt、terminal eventを同じtransactionで収束させる。実Workerを使ったunsupported Providerの回帰contractでterminal phase、Session解放、次Run admissionまで確認した。
- 同reviewでは、model省略retryのdurable snapshotがsource Runのmodelを保持する一方、Provider実行がcurrent Thread modelを使う不一致も確認した。Adapter入力へmodel provenanceを追加し、`thread/resume`と`turn/start`へsource modelを指定する。`A -> B -> retry A`のThread履歴とhidden source modelを回帰contractへ追加し、明示されたhidden modelは引き続き拒否する。
- 後続のtargeted reviewでは、Binding owner上限到達を決定的なApplication failureではなく`interrupted`へ分類する不一致を確認した。Thread作成と再開の両経路を`dispatch_not_sent`、`failed`、`application`へ揃え、Provider Turnを送らずにterminal化することを、実際に`maxTrackedBindings`を飽和させて確認した。
- 最終closure reviewでは、Thread mutationの`not_sent / invalid_input`を処理段階だけで`interrupted`へ分類し、Turn mutationのshutdown中`not_sent / aborted`を`failed`へ分類する不一致を確認した。Provider mutationの結果codeと、結果を最初に観測した時点のshutdown / transport contextを共有classifierで判定し、永続化再試行中に後からshutdownが到来しても分類を変更しない。Thread作成・再開の決定的入力失敗は`failed / application`、送信前のshutdown abortは`interrupted / application`へ収束する回帰contractを追加した。
- 同reviewのtargeted closureでは、cold model catalog取得のtimeout、接続断、Provider rejectionを一律の`capability_unavailable`へ縮退させ、terminal分類の根拠を失う問題を確認した。AdapterはProvider mutation送信前のcatalog失敗について、未送信のtransport codeまたはProvider rejection codeを型付き結果へ保持する。さらにconnection failure eventがmutation結果より先に到着した場合も、最初のterminal transport codeをAdapter ownerへ保持し、後続の未送信Thread / Turn mutationへ同じcodeを返す。runtimeはtimeoutと接続断を`interrupted / transport`、Provider rejectionを`failed / provider`、未対応model tupleを`failed / application`へ収束させる。
- 後続のtargeted re-reviewでは、Provider mutationのtransport応答を待つ間にAdapter固有のfatal eventが先着すると、close由来の`request_not_sent / write_rejected`が保存済みterminal causeを上書きする問題を確認した。Adapterのread / mutation settlement境界は、後着結果が外部副作用なしの`not_sent`である場合、generationが保持する最初のterminal causeへ収束する。cold model catalog待ちとProvider mutation待ちの両経路を、event / resultの順序を固定した回帰contractで確認した。
- Node.js 24.18.0でProvider failure family targeted 139 test、Adapter lifecycle / item / validation targeted 52 test、全854 testとSQLite schema validator、runtime guard、format、module boundary / lint、typecheck、buildを通した。runtime host、Session CLI、Run CLI、Run start / retry、compiled persistenceの各process smokeもGreenである。
- 既存ADR 005とADR 013を現行contractへ更新し、新しいADR、schema変更、accepted riskは追加していない。実Codex App Serverとのprocess E2EとUnix native executable経路は今回のdeterministic smokeの対象外である。

## 2026-07-29: CP3 Run start / retryのshutdown event drainとstartup failure分類

- shutdownとgeneration退役では、Adapter closeで新規受信を停止した後、close開始前に正規化済みのevent queueをEvent Serviceへdrainし、その完了後にgenerationを解放する。Providerが報告済みのterminal outcomeとfinal assistant Messageを、shutdown由来の`interrupted`へ置き換えない。
- Adapter closeは、受信済みevent queueとpending connection failureを保持する。close後もconsumerは`nextEvent()`でqueueをdrainでき、queueが空になった後にclosedを受け取る。
- Provider runtime startup failureを`configuration`、`capability`、`application`、`transport`、`process`へ分類する型付き境界を追加した。設定不備とcapability不一致はpre-dispatchの`failed / application`、process開始失敗は`interrupted / process`、shutdownとの競合は`interrupted / application`へ収束する。cleanup失敗はprocess ownerが未解決なため`process`を維持し、successor起動を拒否する。
- shutdown中に先行eventの永続化を待たせ、その間にfinal付きterminalをqueueへ追加する回帰contractを追加した。Adapter close後のterminal drain、設定未指定、非対応CLI version、process起動失敗、startup cleanup retryもそれぞれ直接検証した。修正前はterminal消失と決定的failureの`interrupted`分類を再現し、修正後は期待するterminalとfailure tupleへ収束した。
- 独立specialist reviewでは、Event Serviceの受理失敗時に取得済みeventと後続queueを破棄できる反例、native executableの形式検証がcurrent OSを区別しない反例、magicだけを持つ切断fileと有効entryの後ろに破損entryを持つfileをnative artifactとして受理する反例を確認した。runtimeは取得済みeventをEvent Serviceの受理完了まで保持し、同じeventから再開して残queueをdrainした後にだけgenerationを解放する。Event Serviceはqueued operationのrejectだけではAttemptとfrozen commandを破棄しない。Codex executableはWindowsのPE header、optional header、全section、LinuxのELF identity、header、全load segment、macOSのthin / fat Mach-O header、load command、section count、slice境界を検証し、wrong-OSまたは宣言構造の一部が切断されたartifactを設定失敗へ閉じる。
- Node.js 24.18.0でRuntime / Event Service / Adapter targeted 127 test、全865 testとSQLite schema validator、runtime guard、format、module boundary / lint、typecheck、build、diff checkを通した。runtime host、Session CLI、Run CLI、Run start / retry、compiled persistenceの各process smokeもGreenである。Run start / retry smokeでは、非対応CLI versionがProvider mutation前に`failed / application`、起動後のProvider process crashが`interrupted`となる区別を確認した。
- 既存ADR 005がpre-dispatch outcomeの判断を所有し、既存ADR 013とProvider designがconnection generationのowner境界を所有する。直接影響するProvider designを更新し、新しいADR、schema変更、accepted riskは追加していない。

## 2026-07-30: CP3 active Runへのsupplemental input

- Provider-neutralな`ApplicationRunOperations.sendInput`、runtime IPCの`run.send_input`、CLIの`withmate run send-input`を追加した。callerはSession、Run、idempotency key、boundedなMessage contentだけを指定し、Attempt、Binding、Provider、external Turn、runtime generationはruntime hostが所有する。
- Repositoryはsupplemental Messageとpending Deliveryのidentityを発行し、同じtransactionで保存する。semantic fingerprintからvolatileなowner tupleを除外し、same-key exact replayでは同じMessageとDeliveryの現在outcomeを返す。別contentまたはscope、欠落したresponse referenceは成功にしない。
- runtime hostはlive owner、active Turn、Run / Attempt / Binding、workspace、generation、capacityをadmission前後で再検証する。freshな`pending -> dispatching` commitを確認したときだけ同じTurnへ`steerTurn`を1回送信し、accepted / rejected / ambiguous / abortedへ単調に収束させる。resolutionの永続化が不明な場合はfrozen commandだけを再試行する。
- client timeout、SIGINT、IPC disconnectはadmission済みDeliveryをcancelしない。public responseとCLI JSONは`pending | accepted | rejected | ambiguous | aborted`と対応するbounded codeだけを公開し、内部owner tuple、external ID、raw Provider response、private pathを除外する。
- isolated fake Codex processを使う`smoke:cli-run-input`を追加した。別CLI processが同じruntime hostとactive Turnを共有し、accepted exact replay、Provider rejection、client-only disconnect後の継続、terminal通知とsteer responseの競合、Provider process crash後のambiguous outcome、timelineへのMessage 1件保存、Provider call 1回を確認した。production profile、既存DB、実Codex executableは使用していない。
- 独立reviewで、handoffを失った先行Deliveryが後続を永続的に停止させるliveness、capacity errorのoperation / Run相関、未解決件数のApplication集計が全履歴を走査するresource boundをblockingと判定した。Event Serviceはretryableなordinal gapをlive owner付きで保持し、先行replayをMessage ordinal順に解決してから後続を再駆動する。Application、runtime IPC、CLIは`run` capacity scopeをsend-inputの対象Runへ完全相関し、start / retryでは受理しない。
- schema v1へ未解決Deliveryだけを保持するpartial indexを追加し、Application集計が履歴全体ではなく最大128件の`pending | dispatching`を探索するquery planをschema validatorで固定した。正式release前のversion 1更新でありmigrationは追加しない。以前のschema hashで作成した開発DBは自動削除せず、必要な場合は明示的に再作成する。
- 最終reviewで、ephemeral Bindingのlive owner tokenがruntimeのprepared dispatchで失われ、Dispatchとsupplemental inputのRepository Gateへ`null`を渡すblockingを確認した。Binding persistence modeとtokenを一つのinternal owner tupleとして保持し、Dispatch begin / resolution、input admission / begin / resolutionへ同じ値を渡す回帰contractを追加した。
- 追加reviewで、完了済みinputがEvent Serviceのqueueへ残り未解決件数を1件多く数える問題と、stale ownerまたは非terminalなRepository拒否で未解決Deliveryをqueueから除去する問題をblockingと判定した。永続Deliveryのterminal確認後だけworkをqueueから除去し、stale-before-beginはProviderへ送信せず`delivery_not_sent`へ収束させる。非terminalな永続化拒否はslotを保持し、Run terminalのaggregate収束を妨げない。上限1で成功後の次preflight、stale owner、非terminal拒否の各回帰contractを追加した。
- 後続reviewで、admission前のdomain failureが`persistence: not_attempted`を持つ共有型と、runtime / CLI projectionが`rejected`だけを受理する契約不一致をblockingと判定した。domain failureの許可statusを共有境界へ集約し、live owner不在とruntime capacity超過が元のdomain errorのままruntime IPCとCLIを通過する回帰contractを追加した。
- 同reviewでは、Provider受理済みinputのresolution永続化が不明な間にterminal eventが先行すると、Run terminal transactionがDeliveryを`ambiguous / process_unknown`へ収束できる競合もblockingと判定した。Event Serviceはterminal commandを凍結した後、`completeRun`より先に同じinput resolution commandをexact retryし、最終Delivery outcomeが`accepted`のままになることを検証する。
- 独立projection reviewで、共有domain failure規則を`run.output_export`へ一般化した結果、read後だけ成立する`not_found`と`payload_unavailable`を`not_attempted`で受理する問題と、`run.send_input`が他operation専用のdomain codeを受理する問題をblockingと判定した。export固有のfailure timingを維持し、send-inputのdomain code allowlistを共有境界へ置いた。CLIではpersistence status間の予約fieldと、detailsを持てない単純domain errorもexactに拒否する。
- 独立lifecycle reviewで、generation release intentをinput resolution再試行より後に保存するため、永続化回復後もreleaseが自動再開されずAttemptがactiveのまま残る問題をblockingと判定した。release intentを再試行前に凍結し、background retryから自動再開する。begin再試行が新しい`delivery_not_sent` resolutionを作った場合も、その永続化確認前にterminal処理へ進まない回帰contractを追加した。
- Node.js 24.18.0で全904 testとSQLite schema validator、runtime guard、format、module boundary / lint、typecheck、build、diff checkを通した。runtime host、Session CLI、Run観測CLI、Run start / retry、Run supplemental input、compiled persistenceの各process smokeもGreenである。
- 既存ADR 013とSession / Run / Message、Provider、Persistenceの各designが判断理由と非局所制約を所有している。直接影響するschema v1 manifestとPersistence designを更新し、新しいADRとaccepted riskは追加していない。
- CP3にはactive cancelとruntime host再起動時のRun全体の復旧が残る。approval / elicitation response、child Session follow-up、GUIは今回のscope外である。

## 2026-07-30: CP3 active Run cancel

- Provider-neutralな`ApplicationRunOperations.cancel`、runtime IPCの`run.cancel`、CLIの`withmate run cancel`を追加した。callerはSession、Run、idempotency keyだけを指定し、Attempt、Binding、Provider、external Thread / Turn、runtime generationはruntime hostが解決する。
- Repositoryはcurrent owner tupleを再検証し、fresh active cancelの`canceling`遷移、request timestamp、Run response referenceを持つidempotency recordを同じtransactionで保存する。terminal Runへのfresh cancelはRunを変更せずreferenceだけを保存し、same-key exact replayはRunの現在のdurable outcomeを返す。
- runtime hostはdurable admission後だけ同じTurnへ`interruptTurn`を最大1回送る。request successはterminalと扱わず、同じRun / Attempt / Binding / execution / generationへ相関した`turn/completed(interrupted)`だけを`canceled`とacknowledged timestampへ写像する。natural completed / failedと相関不能なinterruptedは実際のoutcomeを維持する。
- cancel request後のterminal outcomeにもrequest factを保持できるよう、Application projection、CLI projection、schema v1のphase / timestamp invariantを揃えた。正式release前のversion 1更新でありmigrationは追加していない。以前のschema hashで作成した開発DBは自動削除せず、必要な場合は明示的に再作成する。
- client timeout、SIGINT、IPC disconnectはserver-side cancel workを中止しない。cancel admission後は新しいsupplemental inputを送らず、既に開始したinput resolution、RunOutput、terminal commandのdurable closureを同じAttempt chainで完了してからgenerationを解放する。
- isolated fake Codex processを使う`smoke:cli-run-cancel`を追加した。別CLI processからのfresh `canceling`、interrupt 1回、same-key replay、matching interrupted後の`canceled`、request / acknowledgement / terminalの時刻順、terminal no-op、natural completion race、client-only disconnect後の継続を確認する。production profile、既存DB、実Codex executableは使用しない。
- process smokeで、Event Serviceがclass methodのreceiverを失ってCodex Adapterのinterruptを送れない問題と、terminal Runのlive owner退役後にfresh no-op cancelを`not_found`へ誤分類する問題を再現した。Adapter methodはreceiver付きで呼び、live owner不在時はdurable Runがterminalであることを再読できた場合だけterminal-only admissionへ進める回帰contractを追加した。
- 独立reviewで、cancel admissionのcommit後にresponseを失い、live reservationを解放してからmatching `interrupted`が届く順序では、durable cancelとの相関を失う問題を確認した。Event Serviceはcurrent Attemptと一致するterminal通知について永続Runを再照合し、same-key replay前でも`canceled`へ収束する回帰contractを追加した。
- Node.js 24.18.0でactive cancelのtargeted Application / Event Service 63 test、全938 testとSQLite schema validator、runtime guard、format、module boundary / lint、typecheck、build、diff checkを通した。runtime host、Session CLI、Run観測CLI、Run start / retry、Run supplemental input、Run cancel、compiled persistenceの各process smokeもGreenである。
- 既存ADR 013とSession / Run / Message、Provider、Persistenceの各designがowner、phase、recoveryの判断理由を所有している。accepted designを変更していないため新しいADRとaccepted riskは追加していない。
- CP3にはruntime host再起動時のRun全体の復旧が残る。approval / elicitation response、child Session / Delegation cancel、force kill、GUIは今回のscope外である。

## 2026-07-31: CP3 Provider settings / live interaction契約調査

- 実装前の確認で、Providerごとにapproval / sandbox / permissionの意味と選択肢が異なり、Codex固有のUIまたは共通enumを正本にできないことを確認した。ProviderはSession作成時に固定し、`providerId + definitionVersion`で識別するProvider definitionがsettings schema、declarative GUI、canonicalization、dispatch compiler、interaction schemaを一体で所有する判断をADR 015とD-008へ記録した。
- public操作を`withmate run interactions` / `withmate run respond-interaction`、Application操作を`interactions()` / `respondInteraction()`、runtime IPCを`run.interactions` / `run.respond_interaction`とした。interaction responseはkind-discriminatedなclosed unionとし、Provider request ID、raw schema、transport responseを公開しない。response admissionとcancel admissionは同じper-Run mutation ownerで直列化し、write開始後の切断は`ambiguous`として自動再送しない。
- `codex-cli 0.145.0`のstable schemaと公式sourceを確認し、repository外の一時workspace、ephemeral Thread、isolated hooks / local stdio MCP、networkなし、persistent grantなしでinteraction probeを実行した。command / file approvalのaccept / declineはrequest、response、`serverRequest/resolved`、item / Turn terminalまで完了した。turn-scoped permissionとfeature有効時の`request_user_input`はrequest、response、resolved、Turn terminalまで完了した。
- MCPの先行blockedは、MCP tool approvalとserver formが同じ`mcpServer/elicitation/request`で届くのに一つのformとして扱ったことが原因だった。`meta.codex_approval_kind=mcp_tool_call`で別kindへ分け、tool approvalへ永続choiceなしの空content accept、続くserver formへkind固有contentを返すと、fixture response、tool result、`item/completed(mcpToolCall)`、`turn/completed(completed)`まで完了した。direct `mcpServer/tool/call`も完全round tripを再確認した。
- probeは固定command / file target / workspace / question / MCP server・tool・arguments・schemaをexactに照合し、不一致ではacceptを送らない。main 14分 / total 15分のhard deadlineと最大10 Turnを守り、各live実行時のcleanupは`verified`だった。review後はform先行、未知discriminator、failed Turn、対象itemの非completed terminal、fixture lifecycleの不足・余分、同じownerの別kind / owner表現違いrequestを`blocked`とした。cleanup対象から外れたdiscovery clientもtransport履歴へ残し、同じstdout chunkで正常responseの後にoverflowした場合を成功へ戻さない。discovery用を含むApp ServerとMCP fixture processの回収、sentinel、temp root削除がすべて成功した場合だけtop-level `pass`になるよう強化した。Windows cleanupはaudit PIDの再探索を廃止し、起動指示待ちsupervisorを先にJob Objectへ割り当て、fixtureを含むdescendantを同じownerで回収する。Linux cleanupは`Delegate=yes`、`KillMode=control-group`の一時systemd user serviceで専用cgroup v2を作り、`cgroup.kill`をwrite-openした後にだけsubjectを起動する。subject、内側supervisor、`systemd-run` wrapperの終了順に依存せず、保持済みcgroup handleから同じtreeだけを回収する。systemd user managerとdelegated cgroup v2を利用できないPOSIXはsubject spawn前にfail-closedとする。aggregate cleanup budget、非同期temp削除、total deadline watchdogを設け、ownerまたは終了確認に失敗した場合は非0で閉じる。delegated cgroup open失敗とcgroup handle取得後かつsubject launch前の失敗を注入し、partial ownerのunit回収とowner退役を確認した。
- Codex初期interactionの6 kind、snapshot / responseのclosed union、静的な個数・文字列上限を`schema/providers/codex/interaction-v1.schema.json`へ固定した。dynamicなquestion / field IDの一意性、option membershipまたは`allowOther=true`のbounded自由入力、回答cardinality、MCP formのfield集合・requiredness・field固有上限、current owner、`answerable`、許可decisionはruntimeでsnapshotと再照合する。MCP formはacceptのvaluesと値を持たないdecline / cancelを別shapeにし、安全判断へ必要なcontentを上限内へ完全に投影できなければtruncateせずunavailableとする。response admission、cancel、`serverRequest/resolved`、Turn terminalは同じper-Run mutation ownerで順序を確定し、pending handle検証から`write_attempted`のdurable遷移までに解決済みへ変わる隙間を作らない契約へ揃えた。
- Node.js 24.18.0でruntime guard、probe syntax / self-test、通常・follow-up dry run、外部Turn 0のApp Server preflight、interaction schemaのkind / closed-union / persistent-grant静的check、Draft 2020-12 validator compileとfile path / responseのpositive / negative 12 instance、対象文書のformat、diff checkを通した。self-testはpublic responseのsnapshot照合、prototype由来keyのrequired判定、Unicode code point境界、上限超過時のfail-closed projection、workspace相対path、Provider item ID除去、missing-terminalとterminal / prewarm / payload mismatch診断のallowlist集約、MCP二段階interactionのresolved順序と別kind / owner表現違い / 追加・再配送request / duplicate resolved拒否、未知audit recordのexact lifecycle拒否、App Server JSONLのnewline前1行256 KiB・4,096件・UTF-8合計4 MiB上限、上限超過後の単調なtransport failure、停止済みdiscovery clientのtransport履歴、MCP fixture audit writer / readerの共有file全体に対する64件・64 KiB上限、未知CLI引数拒否、Windows Job ObjectとLinux delegated cgroupの実process tree回収、Windows Job assignment失敗後のsupervisor回収、primary terminationとreleaseの両方が失敗してもwatchdogで終了コード124へ収束し、外側processがwrapper・supervisor・subject・descendantの消滅を確認するtestを含む。file change projectionはC0 / C1制御文字とbidi controlを拒否する。
- Linuxではsubject先行、内側supervisor先行、`systemd-run` wrapper先行、App Server client停止中のwrapper先行、短命subject後のprocess churnと無関係guard processへの非干渉に加え、`cgroup.kill` write失敗時にprobe自身が監査済みの正確なunitを停止してinactiveまたは不存在まで確認するcaseを、公式archiveのSHA-256を検証したNode.js 24.18.0でWSL2 Ubuntu上の実process self-testとして通した。Windowsは39 case、Linuxは46 caseで、Linux self-test後の残存unitは0だった。live実測は強化前に10 Turnの上限まで完了しており、強化後の再実測は行っていない。production TypeScript、SQLite schema、CLI commandは変更していない。Provider timeout、resolved後の重複response、write直後の切断、interaction responseとcancelの競合は実装時のApplication contract test / runtime probe Gateとして残る。CP3は進行中のままである。

## 2026-08-01: CP3 live interaction追加実測

- `codex-cli 0.145.0`の追加live probeを当初計画した50 Turn実行した。この50は当時のbatch計画であり、App Server検証全体の上限ではない。permissionとuser inputの専用mode、MCP二段round trip、bounded client wait、response送信直後・遅延後・resolved後のstdio切断、response / cancel競合、同一process上の2 active Runを検証し、全caseでtransport終了、process tree回収、sentinel不変、temp root削除を確認した。
- response / cancel競合はresponse先行4回とinterrupt先行4回で、interaction 1件、resolved 1件、`turn/completed(interrupted)` 1件、副作用0件へ収束した。同一process上の並行実行はtoolなし2組と片方がpending approvalの2組で、各Runのterminal 1件、cross-owner resolution 0件を確認した。Application側のdurable admissionを代替しないため、response admissionとcancel admissionを同じper-Run mutation ownerで直列化する設計を維持する。
- stdio切断ではresponse送信後または`serverRequest/resolved`後でもterminal eventを失い、観測した固定markerの副作用はなかった。`resolved`をcommand完了の証拠にせず、`write_attempted`後にterminal certaintyを失ったresponseは`ambiguous`として自動再送しない。Provider側timeout、duplicate responseの独立した受理・拒否結果、切断後の一般的な副作用照合は実装Gateとして残る。
- 生成bindingと実測から、permissionとuser inputはrequestのitem IDを持つ一方で専用`ThreadItem` terminalを持たず、MCP interaction requestはexactなThread / Turn IDを持つ一方でitem IDを持たないことを確認した。probeをmethod別owner / terminal条件へ修正し、permission / user inputは専用mode各2回でrequest、response、resolved、Turn completedを再確認した。最後の2 TurnではMCPへitem IDまたは先行`item/started`を要求するprobe条件が安全に`blocked`となり、副作用0とcleanup完了を確認した。その時点では追加live passを行わず、後続のLuna継続検証で修正版MCP round tripを再実測した。production TypeScript、SQLite schema、CLI commandは変更していない。CP3は進行中のままである。
- 後続の全live Turnは`gpt-5.6-luna` / `high`を明示し、`ultra`を使用しなかった。preflightでexact model ID、default effort `medium`、supported effort `low` / `medium` / `high` / `xhigh` / `max`、visible model 7件、hidden込み8件、hidden 1件、Lunaがvisibleであることを照合した。`modelProvider/capabilities/read`は`imageGeneration`、`namespaceTools`、`webSearch`がすべて`true`だった。
- 修正版MCP二段round trip 1 Turn、resolved後duplicate response 1 Turn、8並行Turn、assistant phase 3 Turn、最終10並行Turnの計23 Turnを追加実測した。MCPはrequest 2件、resolved 2件、fixture response、tool result、item / Turn terminalまで完了した。duplicate responseは追加error、追加resolved、追加terminal、副作用を生じなかったが、再送自体への独立ACKがないため受理 / 拒否は断定しない。10並行Turnは各start / terminal 1件、interaction 0件、cross-owner event 0件で、開始からterminalまでの最大同時active数10を示した。10を支持下限とし、絶対上限、backpressure、resource limitはhost / account / model / version依存の実装Gateとして残す。
- contract review後のclosureではLuna / `high`でさらに74 Turnを実行し、この継続検証を少なくとも97 Turnまで拡張した。optionと自由入力Other、command / fileのaccept / decline、permission、通常時とprewarm後のMCP二段round trip、resolved後duplicate、assistant phase、2件と10件の並行実行、pending interactionのsibling分離、response / interruptの両順序、response直後・遅延後・resolved後のstdio切断をexactなThread / Turn terminal相関で再確認した。probe実装不備で安全にblockedとなった10 Turnとmodel tool-choice不一致でblockedとなった4 Turnはprotocolのpass根拠に数えていない。external Turn 0のdirect MCPはfixture監査の最終書込みをboundedに待って完全suffixを確認した。各invocationはtransport、process tree、sentinel、temp root cleanupまで`verified`だった。
- Lunaのassistant phase 3 Turnは各Turnで`commentary` 1件、`final_answer` 1件、`null` 0件だった。stable schemaが許容するfield欠落 / `null`のfallbackはsynthetic mapper contractで固定する。未知notificationはsynthetic client self-testでbounded / sanitizedな`other`診断へ変換した後も既知terminalの処理が継続し、public diagnostic projectionへraw method / payloadを含めないことを確認した。受信時の内部bufferからraw wire messageが直ちに消去されることは検証対象外である。
- CAS-001〜CAS-016は完了した。Provider timeoutのwire上の確定、duplicate responseの独立ACK、terminal eventを失った切断後の一般的な副作用照合、絶対並行上限、runtime host local IPCはApp Server protocolだけでは確定しない境界として、client deadline、idempotency、`ambiguous`収束、host所有limit、別のWithMate IPC契約へ配置した。CAS-017は`0.145.0`でもWindowsでUnix限定として拒否されたため`blocked`を維持する。
- 最終deltaでは、unknown notificationのraw methodがproduction Adapterの`provider_metadata.kind`から永続化・公開される経路を修正前の2 testで再現し、canonical Adapter境界で固定`other`へ正規化した。internal diagnostic correlationと後続terminal処理は維持した。Adapter event-pump 57 test、typecheck、lintを通し、公開metadata outputへraw method、Thread / Turn / item IDを含めない回帰contractを追加した。
- probeのerror pathに残っていた4か所のterminal waitをexact Thread / Turn tupleへ揃えた後、Luna / `high`でさらに38 Turnを実行した。MCP二段round trip、optionと自由入力Other、10並行、response / interruptの両順序8件、command / fileのaccept / decline、assistant phase、permission / user input / MCP follow-upをinvocation単位でpassし、external Turn 0のdirect MCPも完全fixture suffixまで確認した。固定commandを選ばないmodel tool-choice mismatch 3 Turnはacceptを送らずoverall `blocked`とし、protocolのpass根拠へ数えていない。全invocationでtransportとcleanupを`verified`とした。
- 共有preflight / process ownerへ収束する直前にさらに36 TurnをLuna / `high`で実行し、MCP、option / Other、10並行、response / interrupt両順序と追加interrupt先行、command / file、resolved後disconnect、assistant phase、permission / user input / MCP follow-upをすべてpassした。model tool-choice mismatchはなく、全invocationでtransportとcleanupを`verified`とした。
- 共有化時点のsourceでは3 probeのcapability / catalog / Luna tuple検証を`validation-model-preflight.mjs`へ、Windows Job ObjectとLinux delegated cgroupによるprocess tree ownershipを`probe-process-owner.mjs`へ集約した。Windowsで通常回収、Job assignment失敗、controller先行、cleanup failure、temp削除順の実process self-testを通し、3 probeの外部Turn 0 preflightもpassした。その後、App Server内でLuna / `high`を明示した40 Turnを実行した。runtime 3 Turn、recovery 3 Turn、interaction 34 Turnのうち39 Turnが対象contractをpassした。残る1 Turnは固定commandを選ばないmodel tool-choice mismatchで、acceptを送らず副作用0、transport / cleanup `verified`で安全に`blocked`へ閉じた。interactionはMCP、option / Other、10並行、response / interrupt各4件、command / file、resolved後disconnect、assistant phase、permission / user input / MCP follow-upを現行sourceで再確認し、direct MCPもexternal Turn 0でpassした。recoveryはpreflight 3回と実Turn 3回の一致、completed resume、active disconnect後の`interrupted`収束、cleanup `verified`を確認した。runtimeはowner cleanupとtemp削除後だけreportを出力し、3 caseすべてpassした。`ultra`は使用していない。
- 共有化時点の差分に対してNode.js 24.18.0でruntime guard、対象文書とsourceのformat、module boundary / lint、typecheck、Adapter event-pump 57 test、全938 testとSQLite schema validator、Ajv 8.17.1によるDraft 2020-12 interaction schemaのstrict compile、build、共有process ownerの実process self-test、probe syntax / interaction 66 self-test / runtime・recovery self-test / dry run / Luna catalog preflightを通した。
- accepted risk `CP3-APP-SERVER-R1`: Linux live probeでsubject起動後に`cgroup.kill`と監査済みexact unitへのrelease fallbackが両方失敗すると、probeは`cleanup=failed`と非0または124で終了するが、単体ではunitまたはdescendantの不残存を保証できない。発生は終了statusとcleanup結果で検知でき、通常cleanup failureまたはhard watchdogが出力するboundedな`cleanupRecovery[]`のexact systemd user `unitName`を外側ownerまたはoperatorが停止してinactiveまたは不存在まで確認すれば回復できる。投影対象はprobe生成規則へ一致するunit名だけであり、永続データ、secret、個人データへの影響はなく、WindowsのJob Object cleanupには影響しない。Linux liveをrelease Gateへ含める前、またはLinuxで通常live実行を運用化する時点で、self-testと同等の外側cleanup wrapperを追加して再判断する。
- 独立reviewで、共有preflightのmalformed `hidden` / default、visible / complete集合の不一致を誤って受理できること、runtime / recoveryのprocess owner起動待ちとprobe全体がdeadline外であること、recoveryの取得statusが機械assertされていないことをblockingと判定した。共有preflightをexact validationへ変更して独立negative self-testを追加し、runtime / recoveryへmain 14分、cleanupを含むtotal 15分のdeadlineとhard watchdogを適用した。起動指示前で停止する実process self-test、completed readの`notLoaded`、resume後の`idle`、completed / interrupted Turn、exactなThread / Turn履歴のnegative contractを追加した。
- 修正後にApp Server内でさらに76 TurnをLuna / `high`に明示して実行し、`ultra`は使用しなかった。現行contractを最後までpassしたinvocationはruntime 9 Turn、recovery 12 Turn、interaction 36 Turnの計57 Turnである。修正途中の誤ったrecovery status期待で非passとなった2 invocation 6 Turnと、model tool-choice mismatchでacceptを送らずrace probeが安全に`blocked`となった2 invocation 13 Turnはprotocolのpass根拠へ数えていない。最終recoveryはcompleted read `notLoaded`、resume `idle`、active disconnect後`interrupted`、preflight回数と実Turn数の一致、cleanup `verified`を確認した。最終raceはresponse先行4件とinterrupt先行4件の全caseが副作用0、terminal `interrupted`、cleanup `verified`でpassした。
- 後続のtargeted reviewで、recovery履歴が期待Turnに加えて別Turnを含む場合を受理できたことと、runtime / recoveryの同期temp削除がtotal watchdogをblockできたことをblockingと判定した。期待Turnだけのexact 1件を要求するnegative contractへ揃え、process tree回収確認後のtemp削除をremaining total deadline付きの非同期処理へ変更した。さらに、削除waitのtimeout後に未収束filesystem I/Oだけが残るとowner数だけでhard watchdogを解除できる反例をblockingと判定した。deadline確認後に削除を開始するthunk、未収束deadline operationの追跡、absolute total deadlineへ揃え、ownerが0件で削除I/Oだけが残る外側process self-testが短縮watchdogで終了コード124となりpassを出力しないことを確認した。extra Turn修正後のrecovery 3 Turnと、cleanup変更ごとのruntime / recovery各3 TurnをLuna / `high`で再実測して、status、preflight回数、process tree回収、temp削除までpassした。
- accepted risk `CP3-APP-SERVER-R2`: `runtime-contract-probe.mjs`と`recovery-probe.mjs`はNode.js `readline`が改行を返した後にだけJSONL line / eventを処理するため、ローカルApp Serverが改行なしの出力を継続するとprobe processのmemory使用量を事前上限で閉じられない。対象はrepository外の一時workspaceで動く調査probeに限られ、production Adapter、永続DB、secret、個人データへは影響しない。異常終了または非0終了で検知し、probe processを終了して再実行できる。runtime / recovery probeをunattended CIまたはrelease Gateへ含める前に、interaction probeと同じnewline前byte上限を持つdecoderへ置き換えて再判断する。
- 最終差分ではNode.js 24.18.0でruntime guard、format、module boundary / lint、typecheck、build、Adapter event-pump 57 test、全938 testとSQLite schema validator、Ajv CLIによるDraft 2020-12 interaction schemaのstrict compile、probe syntax、共有preflightのnegative self-test、interaction 69 self-test、runtime / recovery self-test、通常・follow-up dry runを通した。runtime / recovery / interactionの外部Turn 0 preflightとdirect MCP round tripもpassし、各live probeのtransport / process tree / temp cleanupを`verified`とした。全testをbuild / typecheckと同時実行した最初の試行では、今回の変更外にある10 ms deadline testがread開始前timeoutとなる1件のRedを記録したが、対象28 testの単独再実行と全938 testの単独再実行はいずれもGreenだった。
- fresh-context complete-diff reviewでは、不一致のuser inputへ回答を送りながら`not_sent` / 副作用なしと記録できるprobeの証拠不整合をblockingと判定した。fail-closed処理を共有境界へ集約し、command / file / permission / MCPはprotocol-safeなdecline、user inputは回答なしのexact Turn interruptへ分け、実際の送信、resolved、terminal、pending、副作用をreportへ反映した。5 target methodのwrong-kind / invalid-payload matrixと、secret user inputでresponse 0件、interrupt 1件となるnegative self-testを追加した。修正後にLuna / `high`で9 Turnを追加実測し、8 Turnがpassした。残るcommand 1 Turnはmodel tool-choice mismatchを安全にdeclineし、送信済みresponse、resolved、terminal、副作用0を正確に記録してoverall `blocked`へ閉じた。review修正後の累計は85 Turnで、65 Turnをpass根拠へ採用し、修正途中またはmodel tool-choice mismatchの20 Turnを除外した。全invocationでtransport / cleanupを`verified`とし、`ultra`は使用していない。

## 2026-08-02: CP3 live interaction vertical slice

- Codex Provider definitionに、command approval、file change approval、permission approval、user input、MCP tool approval、MCP server elicitationの6 kindと、settings / interactionのversioned schemaを実装した。Adapterはstable schemaをexact validationし、Provider request ID、Thread / Turn / item ID、raw payloadを公開せず、boundedなopaque interaction snapshotへ投影する。安全判断に必要な内容を上限内へ完全に投影できないrequestは`answerable=false`として回答を拒否する。
- Applicationへ`interactions()`と`respondInteraction()`を追加した。response admission、cancel、`serverRequest/resolved`、Turn terminalを同じper-Run Attempt queueで直列化し、durable admissionと`write_attempted`記録の後にだけProviderへ応答する。same-key exact replay、別keyの競合、resolved / cancel / terminal先行、generation loss、persistence retryではProviderへ再送しない。pending interactionはruntime hostのmemory-only ownerとし、再起動後にDB履歴から回答可能状態へ復元しない。
- SQLite schema version 1へinteraction responseのdurable receiptを追加した。`admitted`、`write_attempted`、`resolved`、`ambiguous`、`not_sent`を単調に記録し、terminal settlement、bounded RunEvent、IdempotencyRecordの完了を同一transactionで確定する。canonical responseやProvider request / response payloadは保存しない。startup repairは未送信を`not_sent`、送信開始済みを`ambiguous`へ収束させ、自動再送しない。
- runtime IPCへ`run.interactions` / `run.respond_interaction`、Operational CLIへ`withmate run interactions` / `withmate run respond-interaction`を追加した。client timeout、SIGINT、stdout failure、IPC disconnectはcallerの待機だけを終了し、durable admission後のruntime ownerとProvider処理を継続する。
- isolated fake Codex App Serverを使う`smoke:cli-run-interactions`を追加した。別CLI processからのsnapshotと回答、Provider response 1回、same-key replay、同一key競合、別key二重回答拒否、resolved-first / response-first、cancel / terminal race、client-only disconnect、terminal後のinteraction消失とlate answer拒否、public outputとfixture logの内部ID・private path・secret非露出を確認した。既存のstart / retry、supplemental input、cancel smokeも同じ最終差分で再実行した。
- 初回Candidateの独立reviewで、durable response ownerより先にAdapter handleがretireする競合、persistence応答消失時のowner喪失、`ambiguous`確定待ち中の`resolved` evidence喪失、0.145.0 request shapeの不足、public displayのprivate path / control文字、raw request ID再利用、file observation aggregate、runtime / schema path差異をblockingと判定した。最初の非同期境界前のopaque reservation、exact command retry、二段階settlement、connection-lifetime request identity、Provider-owned exact codec、各public出口の再検証、prospective aggregate accountingへ修正し、Applicationと実Codex managerを組み合わせた早着resolved競合も回帰contractへ追加した。
- Node.js 24.18.0でruntime guard、format、module boundary / lint、typecheck、全1027 test、SQLite schema validator（20 table、53 index、schema SHA-256 `bd4a6351b485a18bad23e827cd02010594a305c8b011e0be85a3078663bf01fb`）、build、runtime host、compiled persistence、CLI Session / Run / start-retry / input / cancel / interaction smokeを通した。interaction smokeの初回はfake App Serverのcommand approval requestに0.145.0で必須の`startedAtMs`がなく、production codecが正しくfail-closeしたためRedとなった。fixtureをexact contractへ揃えた再実行はGreenだった。以前記録した`smoke:cli-run`の1 ms output-export timeout caseも最終差分の単独再実行ではGreenだった。interaction sliceに未解決のblocking findingはなく、CP3全体はrestart recovery未完了のため進行中を維持する。
- 後続reviewで、definite no-effectのresponse admission失敗からsame-key retryできないこと、queued cancel preflightより後のresponseが先に予約できること、永続化結果のunknown certaintyがretry throwで後退することをblockingと判定した。Provider reservationを保持するretryable admission、cancel preflightの同期barrier、certaintyを後退させないexact write retryへ修正し、各failure timingをApplication contract testへ追加した。
- Codex `0.145.0`の生成型と一次sourceへrequest codecを再照合し、command decisionのdata variant、nullableなuser input deadline / MCP owner / permission option、MCPのform / openai-form / URLとprimitive schemaを「validだが未対応」と「malformed」に分離した。punctuation直後のPOSIX / Windows private pathも共有validator、JSON Schema、CLI / IPC projectionで拒否する回帰contractへ追加した。
- Node.js 24.18.0でruntime guard、format、module boundary / lint、typecheck、全1030 test、SQLite schema validator、build、runtime host、compiled persistence、CLI Session / Run / start-retry / input / cancel / interaction smokeを再実行してGreenだった。Codex `0.145.0`をsession-localな一時runtimeへ隔離し、App Server内でLuna / `high`を明示した62 Turnを追加実測した。60 Turnをpass根拠へ採用し、Lunaが固定commandを意味の等しい別表記にしたrace 2 Turnはacceptせず、安全なdecline、副作用0、cleanup `verified`へ閉じた。別invocationのresponse先行4回とinterrupt先行6回、10同時Turn、MCP二段round trip、切断、duplicate、runtime、recoveryはpassした。通常PATH上の`0.146.0`はversion gateでlive Turn開始前に拒否し、隔離runtimeは検証後に削除した。
- 同一Candidateへの後続targeted reviewでは、scheduled retryをまたぐと一度観測したunknown effectを忘れること、retryableなno-effect admissionをpublic responseでretry不能と表示すること、Applicationのdirect public operationだけProvider snapshotの再検証を行わないこと、JSON Schemaだけmixed-caseの`file:` URIを許可することをblockingと判定した。さらにCodex `0.145.0`のcommand decision data variantでnested fieldをcamelCaseとしていたこと、MCP tool approvalのscalar `persist`を回答不能にしていたこと、protocol-validなMCP schemaがresource上限を超えるとmalformedへ反転することをblockingと判定した。
- interaction responseのfrozen commandごとにunknown観測を保持し、後続のno-effect retryでcertaintyを後退させずexact replayを継続するよう修正した。retryable admission failureはpublic retryabilityとsame-key ownerを維持し、nonretryable failureはreservationを解放する。Application `interactions()`でもProvider Registryのcanonicalizerを適用し、schemaのmixed-case `file:` URI、公式snake_case decision field、scalar `persist`、question / option / enum / multi-select / form propertyのresource thresholdをruntimeとexecutable contractへ揃えた。Node.js 24.18.0でtargeted 196 test、全1033 testとSQLite schema validator、runtime guard、format、module boundary / lint、typecheck、build、8 process smoke、interaction probe self-test、diff checkを通した。
- lifecycleのtargeted reviewで、回復不能なpre-write admission失敗がProvider reservationだけを解放し、公開interactionとProvider requestを未収束のまま残す経路をblockingと判定した。Applicationのper-Run ownerで公開interactionを最初の非同期境界前に閉じ、reservationを保持したままTurn interruptを最大1回だけhandoffし、release完了後にRunを`interrupted` / `failureOrigin: application`へ収束する専用状態を追加した。release失敗時はadmissionとinterruptを再実行せずreleaseだけを再試行し、Provider terminal / resolvedとの競合とterminal persistenceの応答消失でもProvider response 0回、interrupt最大1回、同一terminal commandのexact retryを維持する。nonretryableなPersistence failure、domain rejection、durable admission後にwrite-attempt markerを確立できず`not_sent`へsettleする兄弟経路を同じcontractへ揃えた。
- schema / projectionのtargeted reviewで、MCP tool approvalの`persist`配列が値を検証せず、空、未知、重複、逆順を回答可能としていた経路をblockingと判定した。Codex `0.145.0`で確認した未指定、scalar `session` / `always`、exactな`["session", "always"]`だけを`meta` / `_meta`の両入口で受理し、persistent grantをProvider responseへ追加しないclosed codecとnegative contractへ修正した。
- 統合後のNode.js 24.18.0でtargeted 429 test、全1038 testとSQLite schema validator（20 table、53 index、schema SHA-256 `bd4a6351b485a18bad23e827cd02010594a305c8b011e0be85a3078663bf01fb`）、runtime guard、format、module boundary / lint、typecheck、build、8 process smoke、interaction probe self-test、diff checkを通した。live App Serverの追加Turnは実行しておらず、同じsliceですでに実施したCodex `0.145.0` / Luna `high`の62 Turnをprotocol根拠、fake App Server interaction smokeとApplication / Adapter contract testを最終product差分の実行根拠として分けて扱う。
- lifecycleの後続targeted reviewで、pre-write marker失敗後のsettlementがretryableな`effect: none`または`ok: false`となると、frozen commandを保持したまま自動再駆動せず、reservationとactive Runを残す経路をblockingと判定した。retryable failureは同じsettlement commandとaggregate certaintyを保持してbackground retryへ戻し、nonretryable failureはProviderへ送信せずAttemptをfail-closeする。連続failureからdurable `not_sent`、reservation解放、interrupt、Run terminalまで自動収束する回帰contractを追加した。
- 同reviewで、fail-closeとinterrupt latchがresponse work単位であったため、最初のworkのreservation解放に失敗すると、同期予約済みの兄弟workがProvider responseまたは2回目のinterruptへ進める経路をblockingと判定した。fail-closeをAttempt単位の単調なGateへ変更し、最初のfailureで公開interactionを閉じ、全兄弟workを現在のwrite境界に応じた`not_sent`または`ambiguous`のsettlement専用経路へ同期的に封鎖する。interruptはAttemptにつき最大1回とし、queued `resolved` evidenceが既存certaintyを強める場合もProvider responseを送らずdurable settlementだけへ収束するcontractを追加した。
- 最終deltaに対してNode.js 24.18.0でApplication Run Event Service 83 test、全1042 test、SQLite schema validator（20 table、53 index、schema SHA-256 `bd4a6351b485a18bad23e827cd02010594a305c8b011e0be85a3078663bf01fb`）、runtime guard、format、module boundary / lint、typecheck、build、runtime host、compiled persistence、CLI Session / Run / start-retry / input / cancel / interactionの8 process smoke、interaction probe self-test、diff checkを通した。live App Serverの追加Turnは実行せず、Codex `0.145.0` / Luna `high`の62 Turnはprotocol根拠として、最終product差分の実行根拠はfake App Server smokeとApplication / Adapter / Persistence contract testとして区別する。
- lifecycleのCandidate B targeted reviewで、Attemptのinteraction responseをfail-closeした後も、既に予約またはhandoffされたcancelとsupplemental inputが兄弟mutationのGateを通り、同じTurnへの2回目のinterruptまたは追加のsteerへ到達できる経路をblockingと判定した。cancelとinputのowner predicateを同じAttempt単位の単調なfail-close Gateへ揃え、未開始のProvider副作用を禁止する。開始済みの永続化commandは破棄せずexact retryし、input beginが`sendAllowed: true`へ収束しても`delivery_not_sent`をdurableに確定してProviderへ送らない。cancel handoff、input handoff、input begin retryの3つの競合を直接contractへ追加した。
- 最終Candidateに対してNode.js 24.18.0でApplication Run Event Service 86 test、全1045 test、SQLite schema validator（20 table、53 index、schema SHA-256 `bd4a6351b485a18bad23e827cd02010594a305c8b011e0be85a3078663bf01fb`）、runtime guard、format、module boundary / lint、typecheck、build、runtime host、compiled persistence、CLI Session / Run / start-retry / input / cancel / interactionの8 process smoke、interaction probe self-test、diff checkを通した。live App Serverの追加Turnは実行せず、Codex `0.145.0` / Luna `high`の62 Turnはprotocol根拠として、最終product差分の実行根拠はfake App Server smokeとApplication / Adapter / Persistence contract testとして区別する。
- 同じlifecycle reviewの逆順確認で、Provider responseのwrite開始後にcancelが先にTurn interruptを開始し、その後response settlementがnonretryable failureとしてfail-closeへ入ると、fail-close専用latchがcancelのinterruptを認識せず2回目を送る経路をblockingと判定した。interrupt開始状態をAttempt全体の共有ownerへ一般化し、cancelとfail-closeの両経路がProvider call前に同じlatchを同期取得する。cancel結果がaccepted、not-sent、throwのいずれでも後着fail-closeは再送せず、Provider response最大1回、interrupt最大1回、application originのterminalとcancel correlationなしへ収束する対称contractを追加した。
- 最終Candidateに対してNode.js 24.18.0でApplication Run Event Service 87 test、全1046 test、SQLite schema validator（20 table、53 index、schema SHA-256 `bd4a6351b485a18bad23e827cd02010594a305c8b011e0be85a3078663bf01fb`）、runtime guard、format、module boundary / lint、typecheck、build、runtime host、compiled persistence、CLI Session / Run / start-retry / input / cancel / interactionの8 process smoke、interaction probe self-test、diff checkを通した。live App Serverの追加Turnは実行せず、Codex `0.145.0` / Luna `high`の62 Turnはprotocol根拠として、最終product差分の実行根拠はfake App Server smokeとApplication / Adapter / Persistence contract testとして区別する。
- fresh-context complete-diff reviewで、command approvalのcurrent requestが許可するdecision部分集合をpublic snapshotへ投影せず全decisionを回答可能としていたことと、未対応のmodel / reasoning tupleをdurable Run admission後にだけ拒否していたことをblockingと判定した。command decision vocabularyをCodex definitionへ集約し、未指定または`null`だけ全decisionを補い、明示部分集合をschema、snapshot、runtime response validatorへそのまま通す。Run start / retryはexact idempotency replayをread-only probeで先に確定し、新規requestだけProvider capability preflightを行い、unsupported / unavailableではRun、Attempt、Dispatch、Binding、Thread、Turnを作らない。retryでsettings overrideを省略した場合は`modelSelection=inherited`として、過去Runの非selectable modelを履歴継続に限り許容する。
- replay probeとwrite admissionで一致すべきcanonical fingerprint、snapshot、response projectionを`run-admission-idempotency`境界へ集約した。通常startとretryはcallerが再生成したRun identityをfingerprintへ含めず、同じsemantic requestを元のdurable Runへexact replayする。capability preflight失敗後にも二度目のprobeを行い、並行してcommit済みとなったreplayをcapability failureより優先する。
- 修正後はNode.js 24.18.0でtargeted 176 test、全1065 testとSQLite schema validator（20 table、53 index、schema SHA-256 `bd4a6351b485a18bad23e827cd02010594a305c8b011e0be85a3078663bf01fb`）、runtime guard、format、module boundary / lint、typecheck、build、runtime host、compiled persistence、CLI Session / Run / start-retry / input / cancel / interactionの8 process smoke、interaction / runtime / recovery probe self-test、diff checkを通した。App Server内ではexact Codex `0.145.0`、Luna / `high`で56 Turnを追加実測し、54 Turnをpass根拠へ採用した。permission 2 TurnはLunaが`request_permissions`ではなくuser inputを選んだためresponseを送らずinterruptし、terminal `interrupted`、pending 0、副作用0、probe cleanup `verified`へ安全に閉じた。permission round tripは別の2 Turnでpassしている。`ultra`は使用していない。session-localなCLI runtime directoryの削除だけ実行policyに拒否され、削除処理は開始されなかった。
- capability closureの後続reviewで、retryがhiddenなsource modelを`inherited`として継承し、再利用可能なactive Bindingがない場合、durable Run / Attempt / Bindingを作成した後の`thread/start`で`selectable`判定へ戻って決定的に失敗する経路をblockingと判定した。Provider compilerから`thread/start`まで`modelSelection`を伝搬し、explicitだけ`selectable`を要求する。inheritedはcatalog存在、text modality、reasoning effortを検証し、provenanceはwireへ送らない。cold catalog loadはruntime generationが所有し、最初のcaller中断が兄弟preflightへ伝播しないようにした。
- 最終deltaに対してNode.js 24.18.0でtargeted 171 test、全1067 test、SQLite schema validator（20 table、53 index、schema SHA-256 `bd4a6351b485a18bad23e827cd02010594a305c8b011e0be85a3078663bf01fb`）、runtime guard、format、module boundary / lint、typecheck、build、8 process smoke、model preflight / interaction / runtime / recovery probe self-test、diff checkを通した。25 msの再試行中間状態を壁時計で判定していたtestは、全testとbuildの同時実行で1回Redとなったため、2回目の永続化呼出しを明示gateで停止する決定的なcontractへ変更し、対象12回と全testを再実行してGreenとした。
- exact Codex `0.145.0`を起動し、App Server内でLuna / `high`のruntime 3 Turnとrecovery 3 Turnを追加実測した。preflightは外部Turn 0でLuna、supported effort、Provider capability、visible / hidden catalogを確認し、6 Turnはすべてpass、transport / process tree / temp cleanupは`verified`だった。hidden historyの`thread/start`はLunaがvisibleであるためlive Turnでは直接実測せず、Adapter / Provider contract testとfake transportで閉じた。`ultra`は使用していない。
- identity / IPC / resourceのtargeted reviewで、command approvalのpublic `display.command`がworkspace absolute pathをそのまま公開し得る経路をblockingと判定した。raw commandはAdapter内部だけに保持し、完全かつ一意に識別できるworkspace rootだけを`<workspace>`へ置換する。外部absolute path、UNC、device path、file URI、home-relative path、parent traversal、raw placeholder衝突、path境界が曖昧なcommandは`unsafe_projection`として`answerable=false`へ閉じる。plain command actionもraw top-level commandと完全一致する`type=unknown`の1件だけを回答可能とし、public snapshotとProvider responseの責務を分離した。
- privacy deltaに対してProvider codec / definition / Adapterの41 test、公開CLI / IPCを含むtargeted 145 test、全1068 test、SQLite schema validator、runtime guard、format、module boundary / lint、typecheck、build、runtime host、compiled persistence、CLI Session / Run / start-retry / input / cancel / interactionの8 process smokeを再実行してGreenとした。schemaとruntime validatorの両方で`<workspace>`だけを許可し、raw Windows / Unix private pathとURIを拒否するcontractを確認した。
- identity / IPC / resourceのtargeted re-reviewで、drive-relative、root-relative、`//`、named-home、token境界をまたぐparent traversal、UNC / device workspace、malformedな`<workspace>` tokenが回答可能となるcounterexampleをblockingと判定した。command approval専用の共有scannerへpath分類とplaceholder境界を集約し、UNC / device workspaceをfail-closeし、`<workspace>`を独立したroot tokenとしてだけ許可する。runtime、JSON Schema、Adapter、public projectionへ同じcounterexample matrixを適用した。
- privacy closureのfocused checkとしてProvider codec / definition / Adapterの41 testを再実行してGreenとした。targeted、static、全test、build、process smokeは最終Candidateを固定する前に同じsource状態で再実行する。
- privacy closureを含む最終差分に対してNode.js 24.18.0で公開CLI / IPCを含むtargeted 145 test、全1068 test、SQLite schema validator（20 table、53 index、schema SHA-256 `bd4a6351b485a18bad23e827cd02010594a305c8b011e0be85a3078663bf01fb`）、runtime guard、format、module boundary / lint、typecheck、build、runtime host、compiled persistence、CLI Session / Run / start-retry / input / cancel / interactionの8 process smokeを再実行してGreenとした。
- Provider definition / projectionのspecialist reviewで、workspace root直後の空白またはquote連結を完全なpath境界として扱い、workspace外の実体を`<workspace>`配下に見せられる経路と、generic displayがdrive-relative、single-root backslash、forward-slash UNC / multi-root slash、named-home pathを公開できる経路をblockingと判定した。workspace置換を完全なpath tokenまたはquote literalとして証明できる場合だけに限定し、同じprivate / ambiguous path familyを共有display scanner、command scanner、JSON Schema、Interaction Managerの反例contractへ揃えた。safeなHTTP / HTTPS URL、`HEAD~1..HEAD`、相対引数、正しく引用された空白入りworkspace pathは維持する。実装担当のfocused 41 test、全1068 testとSQLite validator、lint、typecheck、対象formatとschema compileはGreenだった。
- 同reviewのtargeted closureで、workspace pathより前のclosed / escaped quote連結をruntimeが回答可能にする経路と、JSON Schemaだけが`@<workspace>`、`<workspace>$suffix`、quoted-root suffix、`*`、`?`、`+`、angle bracket連結を受理する不一致をblockingと判定した。shell dialectを推測せず、escaped quoteを含む表現とquote連結をruntimeでfail-closeし、placeholder前後の許可境界をSchemaへ揃えた。runtimeとSchemaの共通反例matrix、Adapterでの`answerable: false`、raw Providerへのdecline responseを回帰contractとして追加した。Provider codec / definition / Adapterのfocused 41 test、全1068 test、SQLite schema validator、module boundary / lint、typecheck、対象format、runtime / Schema一致matrixはGreenだった。
- 後続のtargeted reviewで、PowerShellのbacktickまたはcaretでescapeされたquoteをliteral終端と誤認する経路と、Schema単独ではquote内prefix、未閉鎖quote、unquoted child後のquote連結を受理する不一致をblockingと判定した。runtimeはbackslash、backtick、caret直前のquoteと、workspace occurrenceより前に別のclosed quoteがあるcommandをshell非依存に証明不能としてfail-closeする。Schemaはbaseのpath / token境界と`allOf`のquote-state境界へ分離し、opening位置、matching close、escape、closing後のtoken境界をruntimeへ揃えた。quoted 211,400件とunquoted 211,400件の有限parity sweepは0 mismatch、focused 41 test、全1068 test、SQLite validator、lint、typecheck、対象formatはGreenだった。別のquoted tokenを先行させるcommandもunavailableへ倒すため可用性は保守的だが、public表示の情報露出を優先する既存contractに整合する。
- Schema quote-stateのclosure reviewで、quoted `<workspace>`直後に異種quote、空白、command delimiterを置くとSchemaだけが受理する対称反例をblockingと判定した。quoted placeholder直後をmatching closing quote、`/`、`\`だけに限定する局所条件を`allOf`へ追加し、single / double quoteの対称6例を回帰contractへ追加した。584,136候補のparity sweepはfalse accept / false rejectとも0、focused 41 test、全1068 test、SQLite validator、lint、typecheck、対象formatはGreenだった。最初の全testでは無関係なWindows listener timeoutが1件発生したが、対象test単独と続く全test再実行はGreenだった。
- fresh-context complete-diff reviewで、resource-limit、secret user input、unavailable projectionをfail-closeするとき、Providerへのdecline responseが`write_rejected`となるか`turn/interrupt`が未送信、Provider拒否、不正responseとなると、Adapter connectionを閉じずpending interactionとRunを待機させ続け得る経路をblockingと判定した。fail-close操作の失敗をAdapterの`protocol_failed`へ昇格し、connection closeでpending snapshotとreservation ownerを解放する。declineの`write_rejected`とinterruptの未送信、拒否、不正responseを注入するcontractは、修正前にconnection close待ちでtimeoutし、修正後にconnection failure、pending解放、既存Run terminalization境界までGreenとなった。
- 同reviewで、live interaction probeがcommand approvalの`availableDecisions`を検証せず、current requestが提示していないdecisionも自己検証上は受理するvalidation gapを確認した。probeのpublic snapshotとresponse validatorをProvider definition / Schemaと同じ動的subsetへ揃え、欠落、重複、未知decision、request外decisionをself-testで拒否する。実Providerのresponse writer飽和とinterrupt拒否はlive injectionしておらず、production transportとAdapterの決定的なfailure injection contractで閉じた。
- 最終差分に対してNode.js 24.18.0で近接236 test、全1071 test、SQLite schema validator（20 table、53 index、schema SHA-256 `bd4a6351b485a18bad23e827cd02010594a305c8b011e0be85a3078663bf01fb`）、runtime guard、format、module boundary / lint、typecheck、build、runtime host、compiled persistence、CLI Session / Run / input / cancel / interaction / start-retryを含む8 process smoke、interaction probe self-test、diff checkを再実行してGreenとした。追加のlive App Server Turnは実行せず、既存のexact Codex `0.145.0` / Luna `high`の証拠をprotocol根拠として、最終product差分はfake App Server smokeとApplication / Adapter / Persistence contractで検証した。
- lifecycle finding familyのtargeted re-reviewで、回答reservation取得後に別interactionのfail-close失敗からconnection closeへ進んでも、reserved request ownerだけをManagerが保持し、close後にProvider responseを1回送信できる反例をblockingと判定した。通常のresolved / terminalは同期取得済みreservationの一回writeを維持し、connection close / shutdownは全6 interaction kindのreserved responseを`not_sent / closed`へ確定してProviderへ送らない別semanticsへ分離した。closeはpending、identity、tombstone、active reservation、raw request参照を解放し、既にwriteを開始したresponseだけは従来のwritten / unknown certaintyを維持して二重送信を禁止する。
- 最新の最終差分に対して近接115 test、全1073 test、同じSQLite schema validator、runtime guard、format、module boundary / lint、typecheck、build、8 process smoke、interaction probe self-test、diff checkを再実行し、すべてGreenだった。実App Serverのwriter saturation / interrupt rejectionはlive injectionせず、決定的なfake transport failure contractで検証した。
- Codex CLI releaseのallowlistとexact version gateを削除し、`initialize`で観測したCLI identityは診断情報だけとして保持する。互換性はstable protocolのruntime decoderが、必須field、既知field、variant競合、resource limitを実payloadごとに検証して決定する。外部stable protocol objectは加算された未知fieldをcanonical projectionから除外して受理し、WithMateが所有するpublic settings、interaction snapshot、responseはunknown fieldを拒否するclosed contractを維持する。Provider definitionのsemantic versionはCLI releaseから分離して`codex-provider-v1`へ統一した。pre-release DBに旧literalが残る場合は明示的な再作成が必要であり、自動migrationまたは自動削除は追加していない。
- 通常PATH上のCodex `0.146.0`を起動し、App Server内でLuna / `high`を明示してruntime 3 Turn、recovery 3 Turn、interaction 62 Turnの合計68 Turnを実測した。64 Turnを直接pass根拠へ採用し、4 TurnはLunaが要求と異なるinteractionまたは同値だがexactでないcommandを選んだため、Provider responseを送らずinterruptし、pending 0、副作用0、cleanup `verified`へ安全に閉じた。該当contractは別invocationですべてpassしている。Windows daemon attachのCAS017だけはstdio child scope外としてblockを維持した。最終product差分はNode.js 24.18.0で近接128 test、全1074 test、SQLite schema validator（20 table、53 index、schema SHA-256 `bd4a6351b485a18bad23e827cd02010594a305c8b011e0be85a3078663bf01fb`）、runtime guard、format、module boundary / lint、typecheck、build、8 process smoke、model preflight / interaction / runtime / recovery probe self-test、diff checkを通した。`ultra`は使用していない。
- protocol / projectionとlifecycle / resourceのspecialist reviewで、既知Thread item variantの兄弟fieldを未知fieldとして捨てて誤分類できる経路、public Provider settingsのgetter / Proxy / custom prototypeをcanonicalizationが読み得る経路、raw event queueが件数だけで最大約1.10 GiBを保持し、終端後も解放されない経路をblockingと判定した。既知variant familyの既知field集合を共有境界で照合し、Provider recordをdescriptor-basedのplain data snapshotへ統一した。JSONL受理時のline byteをevent queueへ伝搬し、prospective aggregateを制限してdequeue時にaccountingを戻す。close準備後の既受理event drainは維持し、consumerが再開不能なterminal disposalで残存queueを破棄する。最初の全testではtransport resource解放が最後のstdout eventより先にqueueを破棄する回帰が1件発生したため、解放位置をterminal disposalへ移し、対象caseを3回、transport系81 testを再実行した。修正後はApplication start / retryを含む近接104 test、全1078 test、SQLite schema validator（20 table、53 index、同じschema SHA-256）、runtime guard、format、module boundary / lint、typecheck、build、8 process smoke、model preflight / interaction / runtime / recovery probe self-test、diff checkをGreenとした。Codex `0.146.0` / Luna `high`の68 Turnはこのdelta前の実測として区別し、delta後のlive Turnは再実行していない。Linux固有のprocess-tree cleanupと実App Server processへのwriter saturation / interrupt rejectionは未実行で、既存のplatform self-testと決定的なfake transport contractを代替根拠とする。
- 同じspecialist reviewのtargeted closureで、nestedなeffective sandbox、command action、dynamic content、web search action、user inputの既知variant兄弟fieldも未知fieldとして捨てられる経路と、event queueからdequeueしたserver requestのraw additive payloadをProtocolSession、Adapter snapshot、pending interactionが回答待ち中も保持し、32件でwire換算約282 MiBへ到達できる経路をblockingと判定した。各nested unionへ既知field集合を与え、既知兄弟fieldは拒否しつつ真のfuture additive fieldはcanonical projectionから除外して受理する対照contractを追加した。server requestはinspection後にraw paramsを段階的に解放し、pending interactionはidentityとresponse capabilityだけを保持する。response closureはraw envelopeをcaptureせず、queued / direct waiter、response、resolution、Turn terminalのexactly-once semanticsを維持する。修正後は近接177 test、全1081 testとSQLite schema validatorをGreenとした。最終Candidateのstatic check、build、process smoke、probe self-testは後続の完了Gateで再実行する。
- lifecycleのtargeted re-reviewで、`releasePayload()`がraw paramsだけを解放し、response capabilityが元requestをcaptureするため、大きなW3C trace contextをpending interactionのresolution / terminalまで保持できる経路をblockingと判定した。ProtocolSessionが所有するraw request payloadへtrace contextも含め、`releasePayload()`とinvalidateでparamsとtraceを同時に解放する。queued / direct waiterの双方で解放後もresponse ownershipとexactly-onceを維持する回帰contractを追加した。修正後はNode.js 24.18.0で近接177 test、全1081 test、SQLite schema validator（20 table、53 index、同じschema SHA-256）、runtime guard、format、module boundary / lint、typecheck、build、runtime host、compiled persistence、CLI Session / Run / start-retry / input / cancel / interactionの8 process smoke、model preflight / interaction / runtime / recovery probe self-test、diff checkをGreenとした。Codex `0.146.0` / Luna `high`の68 Turnはこのresource delta前の実測として区別し、delta後のlive Turnは再実行していない。
- fresh-context complete-diff reviewで、permission approvalをgenericなdecisionへ縮約してrequestされた権限の選択を公開responseで表現できないこと、`not_sent`の`writeAttemptedAt`と`resolutionCode`の組をApplication / IPC / CLIがSQLite CHECKより広く許可すること、CLI helpがsame-key retryと競合key / stale interactionのProvider未送信契約を説明していないことをblockingと判定した。permission responseを`permissions`のduplicate-free subsetと`scope: "turn"`へ変更し、空集合をdecline、非空集合をacceptとして、選択された`fileSystem` / `network` subtreeだけをProvider responseへ再構成する。公開snapshotへraw profileまたはprivate pathを出さず、snapshot外のcategory、未知field、重複、session scopeを拒否する。`not_sent`はpre-writeの`owner_lost_before_write | adapter_rejected`とpost-write-attemptの`transport_not_sent | adapter_rejected`へ閉じ、Application、runtime IPC、CLI、Persistence projectionを同じdiscriminated unionへ揃えた。CLI helpにはsame-key exact retryと、別key、stale、resolved、terminal interactionをProvider送信前に拒否する契約を明記した。
- 修正後はpermission contract 46 test、certainty / IPC / CLI contract 192 test、両方を含む近接287 test、全1084 testとSQLite schema validator（20 table、53 index、schema SHA-256 `bd4a6351b485a18bad23e827cd02010594a305c8b011e0be85a3078663bf01fb`）、Node.js 24.18.0のruntime guard、format、module boundary / lint、typecheck、build、runtime host、compiled persistence、CLI Session / Run / start-retry / input / cancel / interactionの8 process smoke、model preflight / interaction / runtime / recovery probe self-test、diff checkをGreenとした。permissionはrequestされた`workspace_write` / `network`の全subsetをfake transportで検証し、不正なcross-productとsnapshot外選択をnegative contractで拒否した。schema、manifest、ADRの追加変更は不要だった。
- 最終resource / response delta後に通常PATH上のCodex `0.146.0`を起動し、App Server内でLuna / `high`を明示してpermission専用2 Turnとfeature有効follow-up 3 Turnを追加実測した。permissionは全3回で`request -> response -> serverRequest/resolved -> turn/completed`を完走し、user inputとMCP二段round tripもpass、pending 0、副作用0、transport / cleanup `verified`だった。既定featureの総合8 Turnではcommand / file / MCP / bounded waitの6 caseがpassし、permission / user inputの2 caseはrequestが発生せず`blocked`となったが、正しいfeatureを有効化したfollow-upで両方をpassしたため、CLI releaseではなくcapability / featureと実payloadで可否を判定する設計根拠とする。複数category requestから一部だけを許可する実App Server Turnはprobeのworkspace-write-only安全境界外のため未実行で、`0.146.0`生成型、Provider codec、fake transportのsubset contractを代替根拠とする。`ultra`は使用していない。
- certainty / CLI specialist reviewで、Application projectionが`writeAttemptedAt < admittedAt`、`settledAt < admittedAt`、`settledAt < writeAttemptedAt`の不可能な時刻順序を受理できることと、CLI helpがidempotency receiptの保持期限を示さずsame-key replayを無期限と読めることをblockingと判定した。Application projectionで全certainty branchの時刻順序を再検証し、不可能な組を既存のinvalid result errorへ閉じた。CLI helpはreceiptを30日保持し、期間内だけ同じkey / responseでProvider再送なしのcurrent certaintyを返し、期限後は`idempotency_expired`として送信しない契約へ揃えた。
- 修正後はcertainty sibling sweep 280 test、全1085 testを`--test-concurrency=1`で実行し、SQLite schema validator（20 table、53 index、25 constraint rejection check、schema SHA-256 `bd4a6351b485a18bad23e827cd02010594a305c8b011e0be85a3078663bf01fb`）、runtime guard、format、module boundary / lint、typecheck、build、runtime host、compiled persistence、CLI Session / Run / start-retry / input / cancel / interactionの8 process smoke、model preflight / interaction / runtime / recovery probe self-test、diff checkをGreenとした。既定並列の全testは、今回の変更外にあるSession operation deadline testが高負荷時に別々のcaseで各1件Redとなったが、対象単独と直列の全1085 testはGreenであり、再現性のある変更起因failureとは区別する。
- 同じ最終sourceでCodex `0.146.0`のpreflightを再実行し、Lunaがvisible、`high`がsupported、Provider capabilityとhidden込みcatalogが整合することを確認した。Luna / `high`のpermission 2 Turnとpermission / user input / MCP follow-up 3 Turnはすべてpassし、request、response、resolved、terminal、pending 0、transport、process cleanupを再確認した。`ultra`は使用していない。
- Windowsのdefault parallel testは、今回の変更外にあるSession deadlineまたはruntime-host integration testが別々の実行でRedまたは長時間無出力となり、現行Windows CI Gateの決定的な完了証拠にできなかった。製品runtime、certainty projection、Provider再送、永続データへの影響は確認されず、各caseのisolated rerunと直列full suiteはGreenだった。CIが呼ぶ標準`npm test`を`--test-concurrency=1`へ揃え、全testとSQLite validatorを同じ決定的な順序で実行する。変更後の標準`npm test`は1085 testとSQLite validatorをGreenで完了した。test suiteの並列性能改善は、deadlineとnamed-pipe ownershipを保った隔離方法を確立できた場合だけ別の論理変更として再検討する。
- fresh-context complete-diff closure reviewで、Runtime Hostが`run.respond_interaction`へ不正な`partial_success`を返した場合、IPC clientがApplication Serviceでは生成できないeffect certaintyと永続化失敗の組を受理できることをblockingと判定した。interaction responseはsuccessまたはfailureだけを生成する現行contractに揃え、同operationの`partial_success`をRuntime IPC境界で拒否する。敵対的Hostの回帰contractを追加し、修正後は対象14 test、標準`npm test`の全1086 test、SQLite schema validator、lint、typecheck、build、対象format、diff checkをGreenとした。

## 2026-08-03: CP3 runtime host再起動復旧

- runtime hostはSession Files ownerとPersistence Workerを確立した後、Application operationの公開前にlocal startup repairとboundedなrecovery candidate処理を完了する。repairは`dispatching`を`ambiguous`へ単調に進め、外部副作用が未確定のRunを`unknown`へ揃える。candidateはRun作成時刻とIDのkeyset pageで列挙し、複数Attempt、Binding候補の重複、creatorまたはscope不一致をProvider mutationへ渡さない。
- persistent Bindingの`pending` Dispatchだけを既存handoffへ戻す。`creating` Binding、`dispatching`または`ambiguous` Dispatch、ephemeral BindingはProviderへ再送せず、既存terminal transactionで`interrupted`へ収束する。accepted Dispatchはread-onlyなThread履歴が同じThreadの同じTurnを唯一の`in_progress`として証明した場合だけ、Event Service ownerを先に登録してresumeする。別Turn、terminal、欠落、矛盾した履歴は対象Runだけを収束し、共有generation上の兄弟Runを巻き込まない。connection failureまたはgeneration全体の所有権を失った場合だけgenerationを退役させ、Messageやoutputを履歴から補完しない。
- deterministic fake Codex App Serverへprocess間で隔離状態を引き継ぐfixtureを追加した。compiled runtime hostをhard crashし、pre-response Thread作成とTurn送信が再実行されないこと、accepted active Turnが新しい`turn/start`なしで同じTurnへ再接続すること、canceling Runが同じTurnへinterruptを再駆動してProvider terminalとの相関後にだけ`canceled`となることを確認した。terminal化後の再起動ではProvider processとmutationが増えず、別SessionのRunを完了できた。同smokeは連続3回Greenだった。
- 後続reviewで、startup recovery失敗後のProvider closeがretryableなshutdown-pendingになるとruntime hostが同じcleanup ownerを再駆動できずowner claimを解放する問題と、creator Attemptに属するephemeral creating Bindingを診断不整合としてhost全体の起動失敗へ広げる問題をblockingと判定した。startup cleanupは同じRun runtimeのclose完了までclaimを保持して再試行する。ephemeral creating BindingはProviderへ再送またはresumeせず、persistent creating Bindingと同じ`binding_creation_ambiguous`として既存terminal transactionでBinding invalidation、Dispatch abort、Run interruptedへ局所収束する。
- 修正後はclassifier、Application runtime、Repository terminal、runtime host owner lifecycleを含む近接246 test、標準`npm test`とSQLite schema validator、format、module boundary / lint、typecheck、build、runtime host、compiled persistence、CLI recoveryのprocess smokeをGreenとした。実Codex App Serverを使う新しいlive probeは実行していない。
- Node.js 24.18.0でruntime guard、format、module boundary / lint、typecheck、標準`npm test`とSQLite schema validator（20 table、53 index、25 constraint rejection check、schema SHA-256 `bd4a6351b485a18bad23e827cd02010594a305c8b011e0be85a3078663bf01fb`）、build、runtime host、compiled persistence、CLI Session / Run / start-retry / input / cancel / interaction / recoveryの9 process smoke、diff checkをGreenとした。npmはrepository要件`>=10.9 <12`外の12.0.2であり、実行結果はGreenだが環境適合性のvalidation gapとして残す。実Codex App Serverを使う新しいlive probeは実行せず、既存CAS-008 / CAS-013をprotocol根拠、fake process smokeをproduct recoveryの実行根拠として分けた。
- schema version 1とpublic CLI / IPC operationは変更していない。判断はADR 002、005、013が所有するlocal repair、ambiguous pre-dispatch、runtime generation ownershipの範囲内であり、新しいADRと設計文書は追加していない。CP3 Gateのcancel、process crash、app再起動のterminal収束とambiguous Dispatchの自動再送禁止を通過したため、CP3を完了とした。

## 2026-08-09: CP3 Single-Session Vertical Slice Gate

- CP3で個別に閉じた契約を再実装せず、同一Sessionの横断gapだけをRepository matrixと`smoke:cp03-single-session-gate`へ追加した。Repository matrixは`queued`、`starting`、`active`、`canceling`、`finalizing`の各phaseでstart、retry、archive、close、deleteが`session_busy`となり、partial writeを残さず、terminal確定後だけ後続Runを受理することを検証する。
- deterministic process Gateは、同一Sessionのexact replayとdifferent-key競合、input、interaction response、cancel、terminal release、後続Run、hard crash後のexact recoveryを一つのlifecycleで確認する。primary Runがactiveな間もsecondary Sessionのinteractionを進め、cross-Sessionの誤ったRun owner tupleをinput、response、cancelの全入口でProvider mutation前に拒否し、Session projection、Run ordinal、Message履歴、Provider mutation countが混線しないことを直接確認した。
- public境界はinteractionのworkspace pathを`display.command`へ混入させず、Provider Thread / Turn / item / request ID、raw payload、runtime-private path、owner identityをCLI JSONへ出さない。product-owned stderr diagnosticは既存transport contractでcontent omissionを直接検証し、fake Provider audit logをpublic diagnosticの証拠として扱わない。
- 初回specialist reviewで、active Run中のSession projection、cross-Session owner tuple、interaction表示のpath非露出が横断Gateで未観測だったことをblockingと分類し、同じSG-3、SG-4、SG-6 scopeへ追加した。input / interaction responseのrestart再送禁止は、既存のApplication contractとCLI input / interaction / recovery smokeが同じfailure familyを直接検出するため、mega scriptへ重複させず再利用証拠として再実行した。新しいaccepted riskはない。
- Node.js 24.18.0とCorepack経由のnpm 11.6.2でruntime guard、format、module boundary / lint、typecheck、build、全1114 test、SQLite schema validator（20 table、53 index、25 constraint rejection check、schema SHA-256 `bd4a6351b485a18bad23e827cd02010594a305c8b011e0be85a3078663bf01fb`）をGreenとした。runtime host、compiled persistence、CLI Session / Run / start-retry / input / cancel / interaction / recoveryと新Gateのprocess smokeもGreenで、新Gateは最終sourceで連続2回通過した。global npm 12.0.2は変更していない。
- schema version 1、production source、public CLI / IPC operation、accepted concurrency modelは変更していない。CP3は完了を維持し、GUI、複数Provider、並列agent、live Codex probeはscope外とした。既存ADR 005、013、015とdesignが判断理由を所有するため、新しいADRと設計文書は追加していない。
