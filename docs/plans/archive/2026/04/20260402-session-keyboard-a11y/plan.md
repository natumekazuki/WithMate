# Plan

- 状態: 完了
- 目的: Session 周辺 UI のキーボード操作と基本アクセシビリティを改善し、review 指摘 `#1 #2 #5 #11` を解消する

## チェックポイント

1. 対象 UI と現状挙動を整理し、改善方針を設計へ反映する
2. モーダル、listbox、DiffViewer、`@path` 候補の keyboard / focus 挙動を実装する
3. テストと build を通し、manual test / design / backlog を同期する
