# Decisions

## D-001

- 状態: 採用
- 決定: 既存 UI 構造を保ったまま、共通 helper を追加して `Escape`、初期フォーカス、簡易フォーカストラップを各モーダルへ適用する
- 理由: デザイン変更を広げず、review 指摘のクリティカル項目を先に閉じるため

## D-002

- 状態: 採用
- 決定: `@path` 候補では `Enter` を採用キーに寄せ、`Tab` は原則として通常フォーカス遷移を優先する
- 理由: review 指摘 `#11` に合わせて標準的なキーボード UX に寄せるため
