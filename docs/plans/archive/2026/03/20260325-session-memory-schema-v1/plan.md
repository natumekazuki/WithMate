# 20260325 session-memory-schema-v1

## Goal

- `Session Memory v1` の最小 schema を定義する
- 各 field の役割と、なぜ必要かを説明できる状態にする

## Scope

- `docs/design/memory-architecture.md` に `Session Memory v1` schema を追記
- field ごとの責務、更新契機、将来拡張の考え方を整理

## Out Of Scope

- 実装
- DB schema
- Project / Character Memory の schema 詳細化

## Steps

1. `Session Memory` で最低限必要な field を選ぶ
2. schema と field explanation を `memory-architecture.md` へ追記する
3. plan を閉じる
