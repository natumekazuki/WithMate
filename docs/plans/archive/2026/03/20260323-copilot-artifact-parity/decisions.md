# Decisions

## 2026-03-23

- 次の capability は `changed files / diff` を選ぶ
- 直接のユーザー価値は `Details` 復活だが、実装単位としては Copilot artifact parity として扱う
- MVP は Codex と同じ snapshot diff fallback を使い、provider-native `file_change` parity は後回しにする
