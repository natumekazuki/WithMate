# Plan

- task: Character Editor に `character-notes.md` 編集を追加する
- goal:
  - `CharacterProfile` / storage で `character-notes.md` を扱えるようにする
  - Character Editor に `character-notes.md` タブを追加する
  - current docs と test を新しい editor 構成へ揃える

## Steps

1. shared state と storage に `notesMarkdown` を追加する
2. Character Editor に `character-notes.md` タブを追加する
3. test と design docs を更新する
