# Result

- status: completed
- summary:
  - `Project Memory` を `project_scopes` と `project_memory_entries` に分ける v1 persistence model を定義した
  - `git | directory` の project identity と SQLite schema draft を整理した
  - retrieval 用の FTS / embedding は follow-up として切り分けた
- verification:
  - docs-only のため build / test は未実行
- commits:
  - `9f5276b` `docs(memory): Project Memory の保存設計を整理`
