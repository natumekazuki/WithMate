# Plan

- task: character 定義ファイルの標準構成を固める
- goal:
  - `character.md` を実行時 prompt の正本として扱う前提で、標準セクションを定義する
  - `character-notes.md` の役割を分離し、調査メモや改稿履歴の逃がし先を決める
  - 既存の design docs から参照できる状態にする

## Steps

1. 現在の `character-storage.md` / `character-update-workspace.md` / `prompt-composition.md` の役割を確認する
2. `character-definition-format.md` を追加して、`character.md` と `character-notes.md` の標準構成を定義する
3. 関連 design docs の参照と current / target 境界を揃える
