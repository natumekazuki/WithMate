# Result

- status: completed
- summary:
  - Settings の `DB を初期化` を選択式に更新した
  - `sessions / audit logs / app settings / model catalog` を個別に初期化できるようにした
  - 全対象選択時は DB ファイル再生成で schema ごと初期化するようにした
  - Danger Zone UI、docs、tests を更新した
- commits:
  - `b892f01` `feat(runtime): 監査ログ構造化と DB 初期化を改善`
