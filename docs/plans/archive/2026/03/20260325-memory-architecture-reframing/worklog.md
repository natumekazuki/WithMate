# Worklog

- 2026-03-25: plan 作成。既存の `memory-architecture.md` は `Character / Session / Monologue Context` 前提で、current の悩みどころである `Repository` と `Session` の差が前面に出ていないことを確認した。
- 2026-03-25: `memory-architecture.md` を `Repository / Session / Character` の 3 層へ再定義し、`Repository` と `Session` の違いを昇格ルールで整理した。
