# Result

## Status

- 状態: 完了

## Current Output

- Copilot の `Latest Command` と audit `operations` で、shell だけでなく mutating tool も見えるようになった
- `create / edit / replace / move / delete` などは workspace 相対 path を含む summary に正規化される
- `report_intent` のような補助 tool は `Latest Command` に出さない
- renderer 側の risk label も provider-native summary に追従するようになった

## Remaining

- なし

## Related Commits

- 未コミット
