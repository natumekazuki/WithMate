# 決定

- `character-prompt-maker` のうち、定義本体と notes の分離、既存ドラフト尊重、必要時の外部調査、paired image 管理を流用する
- `character.md` は WithMate の current prompt 合成に合わせ、ファイル全体を `# Character` section に入れる前提を維持する
- `character.png` は Character Update workflow の正式対象にし、skill と template に含める
- current app では workspace 内の `character.png` を paired asset として優先認識する
