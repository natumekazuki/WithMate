# 状態

- 完了

# 結果メモ

- current task は `Character Editor` の `Open Folder` button のみを対象にする
- `UpdateSession` 一覧は別途仕様整理してから着手する
- `Character Editor` の既存 action row に `Open Folder` を追加した
- button は character 保存ディレクトリを OS のエクスプローラーで開く
- 対応コミット: `ea4298b` `feat(character): streamline update session workflow`
