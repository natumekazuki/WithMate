# Result

- status: completed
- summary:
  - Memory extraction trigger を `outputTokens threshold` ベースで固定した
  - provider ごとに 1 数値だけ Settings で持つ方針を docs に反映した
  - `compact 前` / `session close 前` を強制 trigger として整理した
- verification:
  - docs update のため build は不要
