# Result

## Status

- 状態: 完了

## Current Output

- `AppSettings` に `Action Dock` auto close 設定を追加した
- default ON の checkbox を Settings Window から切り替えられるようにした
- 通常送信後は `Action Dock` が compact へ戻り、force-expanded 条件がある時は expanded を維持する
- docs / tests / backlog を current 状態へ同期した
- GitHub issue `#30` に対応コメントを追加した

## Remaining

- なし
